# InFu — 项目状态与开发指南（Agent 必读）

> 本文件由 ZCode 在每个新会话自动加载。开发前先读本节，避免重复劳动或方向错误。

## ⚠️ 强制指令（每次会话必须执行）

1. **读取 `docs/ROADMAP.md`**——其中 ⏳ 未完成项是持续待办，**完成之前每个阶段都要知道并推进**（目前高优先级仅剩"沙箱长期升级 microVM"，触发条件未到）。
2. 将未完成的高优先级项纳入本阶段计划；**完成一项即在 ROADMAP 中标记 ✅**，之后后续阶段不再关注它。
3. 本阶段工作完成后，更新 AGENTS.md 与 ROADMAP.md 的状态（已完成项移入历史区）。

## 项目是什么

InFu（Infinite Future）——AI 开发助手。用户用自然语言给 AI 下开发任务，AI 在代码仓库里自主完成：分析项目 → 规划 → 修改代码 → 跑测试 → 输出交付报告。

## 技术栈（已定，勿改）

- **monorepo**：npm workspaces（`packages/shared`、`packages/agent`、`packages/web`、`packages/sandbox-rs`）
- **后端**：Node + TypeScript + **AI SDK v6**（`ai@6.0.250`，配套 provider 3.x；DeepSeek/智谱/通义/Ollama/自定义端点统一走 `createOpenAI({baseURL})`，OpenAI/Anthropic/Google 用官方 3.x 适配器）
- **原生模块**：Rust N-API（napi-rs，`packages/sandbox-rs/`）——Windows 硬沙箱（restricted tokens + job objects）。构建需 MSVC C++ 工具链（VS 2022 BuildTools VCTools 工作负载）；`npx napi build --platform --release` 产出 `index.node`（不入库）。cargo 1.97 + stable-x86_64-pc-windows-msvc
- **前端**：React 19 + Vite + Tailwind v4 + zustand（设计系统来自 ui-ux-pro-max skill：Dark OLED 深色 + 运行绿 #22C55E，图标用 lucide-react，禁用 emoji 图标）
- **服务**：Hono + SSE 流式（端口 4317，冲突自动递增；Web 端口 5174）

## 常用命令

```bash
npm install                    # 装依赖
npm run build                  # shared → sandbox-rs(native) → agent
npm run config                 # 交互式模型配置向导（~/.infu/config.json，Key 不入库）
npm run infu -- "任务" --root <路径> -y   # CLI 跑 Agent
npm run infu -- sessions       # 会话历史（v2.1：每次任务自动落库）
npm run infu -- --session <id> "继续的指令"   # 继续之前的会话（v2.2：消息级重建续跑，完整恢复历史）
npm run infu -- "任务" --fallback-model <id> [--fallback-model <id>...]   # 备用模型降级链（v2.2）
npm run infu -- "任务" --planner-model <id> --executor-model <id> --reviewer-model <id>   # 按角色指定模型（v2.2）
npm run infu -- "任务" --thinking <1-4>   # 思考级别（v2：按模型实际级别数自动映射）
npm run infu -- mcp add/list/remove/status   # MCP 服务器管理（v2.3：工具动态注入执行阶段）
npm run infu -- plugin add/list/remove/status   # 插件管理（v2.3 批 2：JS 模块 = 工具/钩子/技能）
npm run infu -- plugin marketplace/install <id>   # 插件市场（v2.7：内置注册表 + 一键安装，如 install browser-use）
npm run infu -- skill add/list/remove   # 技能管理（SKILL.md 社区标准）
npm run infu -- skill import/export <path|name>   # 技能导入/导出（v2.7：目录复制分享，export <name> [--to 目录]、import <目录>）
npm run probe -- <modelId>   # provider 兼容性探针（v2.2：流式/思考字段/工具调用/长输出）
npm run start                  # 启动 Agent 服务
npm run test                   # 全量测试：npx tsx scripts/test-runner.ts（43 套件，单套件 300s 超时杀树，全量模式 ≥40 套件下限；支持过滤参数）
start-infu.bat                 # Windows 一键启动（服务 + Web + 浏览器）
npm run dev:desktop -w @infu/web   # 桌面 dev：vite 专用端口 5199（与 Web 版常驻 5174 错开）
npm run start -w @infu/desktop     # 桌面端启动（build 主进程 + Electron，dev 模式；详见 docs/DESKTOP.md）
```

## 已完成（勿重复开发）

- ✅ **v5.0 产品增强批（2026-08-18，审计建议清单全量落地）**：**A1 页面级 E2E 套件**（tests/e2e-prod.test.ts：真实服务器 + playwright 加载生产页——CSP nonce 匹配/零 401/零 CSP 违规/React 渲染/主题双阶段，12 断言入 npm test，43→44 套件；startServer 返回 httpServer）；**A2 命令审计 UI**（GET /api/audit + 设置「命令审计」Tab，搜索/仅失败过滤）；**A3 后台会话待处理徽标**（侧栏会话行琥珀数字徽标：审批/提问/计划挂起计数，切过去即弹出）；**A5 bundle 懒加载**（SettingsModal/CodeView/TerminalPanel lazy——首包 2.31MB→1.22MB，gzip 699→355KB）；**B1 TDD 收敛闭环 + B2 交付前自检**（提示词 14/15 条：修复类先复现→修复→验证循环 3 轮无进展换策略；改动后有测试框架则交付前 run_test）；**B4 快速回复模型**（general.quickModelId：寒暄/极短非任务自动用快模型 + 设置 UI）；**C1 会话级临时联网**（egress-allow.ts + run_command/run_test 接线 + /api/egress/allow 端点 + composer 🌐 药丸 10 分钟倒计时，审计 egress-allowed-temp）；**C2 显式 root 自动注册项目**（≠defaultRoot 且未注册时自动 createProject + 提示，只读容器语义不变）；**C3 托盘增强**（动态菜单：运行中任务 + 最近会话 → IPC session:open → 前端加载会话）；**C4 数据一键备份**（VACUUM INTO 一致性快照 + 配置/记忆复制到 backups/infu-backup-<ts>/ + 设置按钮）；**A4 归档事件压缩**（general.compressArchivedEvents 显式选项默认关：超期归档会话压缩为摘要+最近 200 条，rebuild 兼容）；**D3 增量构建**（tsconfig incremental + tsBuildInfoFile 入 node_modules/.cache）。**验证**：44 套件全绿 + 三端 tsc + vite build + cargo check 全过；/api/audit 探针实测。**未做（产品决策/成本）**：B3 UI 树读取、B5 OCR、C5 中英文统一、D1 tokenizer。**⚠️ 补 1（用户提出）**：full 档外传命令不再被断网机制拦截——补齐 run_command 初始 egress 拦截分支的 full 档放行（此前只放行 network=true 路径，直接 `npm install` 仍被拦；run_test 早已同款；审计 egress-allowed-full 照常，非 full 档语义不变）
- ✅ **v4.0 全库审计修复批（2026-08-18，深度代码审计 7.3/10 后逐项修复 + 优化）**：**① 安全**——read_files 批量通道补 isProtectedPath（task-tools.ts readOneFile，与 read_file 单通道对齐——root=home 会话此前可用批量工具整批读出 SSH 私钥/凭据）；Rust Job 句柄清除继承标志（sandbox-rs/job.rs `SetHandleInformation(HANDLE_FLAG_INHERIT, 0)`——原 bInheritHandle=1 使子进程继承句柄，KILL_ON_JOB_CLOSE「崩溃不留孤儿」永不触发，`start /b` 分离进程永久存活）；浏览器导航 SSRF 门禁（browser_navigate/browser_tab_new 复用 isPrivateHostText——云元数据/内网/本机 IP 直写拒绝，localhost/127.0.0.1 白名单）；命令白名单移除 `type*`（Windows 下 type=读文件命令免审批读任意盘内文件）+ home 凭据文件入写保护（.npmrc/.git-credentials/.netrc）；`/api/approvals/bypass` 校验会话存在 + 开启动作落库审计（新 shared 事件 approval-bypass）+ 前端读取令牌后立即删 window 全局；静态托管安全响应头（X-Frame-Options: DENY + CSP frame-ancestors 'none' + nosniff + no-referrer）；桌面主窗口补 will-navigate/will-redirect 守卫（此前仅 guest 有三闸）+ open-external 过滤回环。**② 逻辑**——持久 shell 超时修复（管道 stdin 的  不是中断信号→超时销毁会话重建防输出串扰 + stderr 环形裁剪补漏）；mcp/plugin CLI 本地 saveConfig 删除统一走 registry 原子写；断网策略参数位绕过修复（`git -C <dir> push`/`npm --prefix /x install`/`pip -r x install` 选项前缀容忍）；file_ops rm 目录递归升级 high+requireExplicit（与 run_command rm -rf 同门槛）；session_trace 历史凭据脱敏；memory_write global/replace 提升 medium（跨会话持久注入面）；数据迁移互迁目标先整体改名备份再复制；终端退出注册表清理；rewind/appendEvent 包事务（txnDepth 防嵌套 BEGIN）；git_diff 放行 Windows 反斜杠；project_scan 越界拦截；SENSITIVE_OUT 补 ghp_/xoxb/AIza/ya29/JWT。**③ 前端**——eventTarget 连接结束重置（组件侧错误不再路由到陈旧会话被静默吞）；提交/回滚/编辑忙守卫；计划卡 plansBySession 残留清除；askQuestion 按会话存储；SchedulePane 检查 res.ok；Sidebar 三处裸 rejection 补提示；TerminalPanel 解析 exit 事件 + escBuf 4KB 上限；fetchFsFile/diff 失败抛错；StatsPane 序号守卫；Windows 反斜杠路径归一三处。**④ 测试**——subagent 套件数据目录重定向+固定 confirm 档（94/0，修正 v3.9「43/43 全绿」失实声称——与 mcp/plugin 同型漏网）；新增断言 33 项（断网参数位 8/rm 红线 4/read_files 保护 2/记忆风险档 3/迁回备份 6/脱敏 3/bypass 审计 2 等）；fs-tools/memory 测试补确认档配置。**⑤ 文档**——README 默认档位 full、默认流程单循环 + `--orchestrate`、窗口 1M 校准；PROVIDER-MATRIX/MCP 序号/TECHNICAL-SELECTION（AI SDK→自研客户端、删 suggestOnly/交付报告残留）/ROADMAP 里程碑误导项修正。**验证**：**43/43 套件全绿 1329 断言 0 失败**（新增断言 28 项）+ agent/web/desktop tsc + vite build + cargo check 全过。**⚠️ 补 1（2026-08-18 用户实证回归）**：CSP `script-src 'self'` 拦截内联脚本——令牌注入脚本是内联的 → 生产页重启后全部 API 401「缺少本地令牌」（项目列表消失/无法创建，数据未丢）。修复 = 令牌注入每次响应随机 nonce（CSP 头与脚本 nonce 配对）+ 主题恢复脚本外置 web/public/theme-init.js；真实服务器 E2E 验证通过（无令牌 401/带令牌 200/nonce 匹配）
- ✅ **v3.9 最大审批权限 + 审计修复批（2026-08-18，用户拍板「最大审批权限，不用任何人参与，全自主完成任务，不出现任何审批弹窗」）**：**① 全自主模式（核心）**——默认审批档位 `smart`→`full`（approval/policy.ts DEFAULT_POLICY；注释记录用户拍板）；**full 档语义补齐**——ask_user 自动跳过（server.ts 1333 与 cli.ts 641：emit `{type:"ask-user", autoSkipped:true}` 事件落库审计后 return null，不再挂起）+ 计划确认自动批准（server.ts confirmPlan full 档 emit plan 事件后直接执行）+ 断网策略 egress 放行（tools/index.ts run_command/run_test：`netAllowed=true` + 审计标记 `egress-allowed-full`）；用户 `~/.infu/config.json` approvalPolicy.mode 已同步 `full`（smart 改）。数据安全不降级：受保护路径/SSRF/路径作用域/显式禁用工具仍拦截。**调研依据**（读工作区外 E:\app\deepseek-harness 授权）：权限 = SandboxMode（read-only/workspace-write/danger-full-access）+ ApprovalPolicy（ask/never）双正交旋钮，danger-full-access 捆绑「文件全开 + 审批全拒（never=确定性拒绝）」；InFu full = 自动放行（对齐用户诉求，区别于 harness CI 语义）。**② 审计修复**——M1 run_test 门禁对齐（白名单放行 = isCommandAllowed && !hasShellCombinators && !DANGEROUS；否则 DANGEROUS→high requireExplicit / 普通→medium / 自动检测→low）；M2 SSRF 尾点归一（shared/net.ts isPrivateHostText/isLoopbackHostText 加 `.replace(/\.+$/, "")`——`127.0.0.1.`/`8.8.8.8.` 变体不再绕过）；M3 read_file 敏感路径保护（isProtectedPath 检查，附件 extraReadDirs 豁免）+ `.infu-redirect.json` 精确入写保护 + sanitizeEnv 补 `PASSPHRASE|_PW$|_PASS$|CONNSTR`；M4 net-policy 外传面补全（git push|fetch|clone|pull|remote add|submodule update、npm|pnpm|yarn|bun install|add|ci|update|publish、pip install、powershell -enc、wsl）；C1 compressMessages 加 `force` 参数（loop.ts ensureContextBudget 400 超限强制压缩）+ estimateTokens 计 tool_calls arguments 正文；C2 寒暄短路收窄（orchestrator.ts：toolCount===0 && 无【建议步数】&& planText<200 字 && !任务意图词表）；dangerous.ts 补分开标志变体（`rm -r -f`/`del /s /q` 命中）；CWE-451 轻量：run_command 审批描述 3 处带 cwd。**③ 工具链检查（对照 harness 25 工具 + 主流）**——52 工具链路完整（探索→读取→计划→修改→测试→验证→提交→汇报 + 并行/异步/记忆/技能/视觉/浏览器）；差距项（goal→todo_write 覆盖、notebook/run_code→run_command 可替代、lsp 跳转→lsp_diagnostics 已有）均无需新增。**④ 验证**——43/43 套件全绿 **1301 断言 0 失败**（新增 15 断言：net 尾点 5、compress force+arguments 5、tools read_file 保护 3、win-sandbox-net 2；mcp/plugin 审批触发段因默认档位改 full 连带失败 → 测试显式固定 confirm 档修复，分别 72/0 与 78/0）；agent/web/desktop tsc 全过；CLI agnes 真实端到端（project_scan→read_file×4→list_directory 全自主零弹窗）；用户 config 已 full。**注意**：approval-cache/web-tools 测试会短暂改写 ~/.infu/config.json（备份恢复，已确认恢复后为 full）
- ✅ **v3.8 文档与测试修复批（2026-08-18，用户拍板「文档补补、测试补补就行了」）**：**测试修复 3 项**——① net.test.ts 红套件根治（shared/net.ts `ipv6EmbeddedV4` 对 IPv6 回环 `::1`/完整形式返回 null——原实现使「::1 非内嵌」断言失败，套件自 v3.6 创建起一直红；补 3 断言 62/0）；② fs-tools.test.ts 写保护用例改隔离目录（原 `os.homedir()` 作 root，写保护若回归会删真实 ~/.infu——setDataDirForTest 临时目录 + 「config.json 未被删除」断言，38/0）；③ projects.test.ts 隐藏红套件（v3.6 新增 projBlank 用例后「注册表格式」断言 length 1→2 未同步，自 v3.6 起一直红且退出码 1——21/0）。**测试基建 1 项**——新 `scripts/test-runner.ts`：43 套件清单 + 单套件 300s 超时（taskkill /T 杀树）+ 全量模式 ≥40 套件下限 + 过滤参数 + 失败尾部打印；parseAssertCount 兼容「结果：N 通过 / M 失败」「完成：N 通过，M 失败」「N passed, M failed」三种格式；Windows shell:true（npx.cmd spawn EINVAL）；根与 agent 的 test 脚本均改走 runner。**文档漂移修复 9 处**——README（状态 v3.5→v3.8 43 套件 1286 断言、删 `--suggest` 与 `--no-orchestrate` 已删除 flag、`--no-plan-approval` 保留）；MCP.md（内置 10 工具→52、58 项→72、删 suggestOnly/best-of-n 过期行、顶栏按钮→设置弹窗）；PLUGINS.md（61→78 项、删 suggestOnly）；SUBAGENTS.md（explore 只读 7→15 件）；SANDBOX.md（best-of-n「已实现」→已删除标注、网络缺口更新为命令级断网已落地、删重复参考资料段）；PROVIDER-MATRIX.md（删不存在机制 inferCapabilities.toolCalling、超时 120s→两段式 60s/300s）；TERMINAL.md（审计落盘路径跟随数据目录 + 轮转）；ROADMAP.md 历史区 4 处误导项（computer-use/定时任务/语义检索/浏览器 tab 占位/持久 shell/LSP/read_image 均标注后已完成、开机自启注明 v3.5 起为默认关设置项）。**验证**：`npx tsx scripts/test-runner.ts` 43/43 套件全绿 1286 断言 0 失败（历史首次真实全绿）；审计发现的安全项（type* 白名单任意读、file_ops rm 删根等）按用户指示不修，仅记录。**已知限制**：terminal 套件 node-pty AttachConsole 崩溃噪声（环境限制，退出码 0 非回归）
- ✅ **v3.7 审计修复批（2026-08-18，用户指定 6 项修复 + 附加 1 项）**：**① 压缩剪枝丢弃修复**（loop.ts：if (r.after < r.before) 改为 || r.messages !== messages——剪枝返回原引用时不再误丢弃）；**② CRLF 分帧卡死修复**（chat.ts：split("\n\n") → split(/\r?\n\r?\n/)——CRLF 输出不再卡死等待下一帧）；**③ cron 日/周字段 OR 语义**（schedule.ts：dom 与 dow 均受限时按标准 cron（Vixie）取 OR；错误示例 "0 9 * * 1-5" → "0 9 * * 1,2,3,4,5"）；**④ 终端双字段旁路修复**（server.ts：if (!data) → if (!data && !command)——command 与 data 并存时 data 段同样过危险命令检测，两段均审计；前端 data-only 路径不受影响）；**⑤ 重启重复执行修复**（schedule.ts：tick 在 run 前 markScheduleRun(entry.id, "started") 预留 lastRun——runner 挂起重启不再重复执行）；**⑥ rewind 竞态修复**（server.ts：running 会话回滚 → 400 "会话正在运行，请先停止任务再回滚"）；**⑦ 附加：cron 语法校验正则修复**（schedule.ts：/^\*\/\d+$/ → /^\*\/(\d+)$/——原无捕获组导致所有 */n 表达式被拒，含文档示例）。**验证**：新增 schedule.test.ts 21 项（cron OR 语义/语法校验/预留防重）+ server-api.test.ts 新增终端双字段 4 项 + rewind 3 项；全量相关套件 0 失败（server-api 19/schedule 21/terminal 41/bugfix 39/compress 35/retry 22/loop-opt 14）+ agent tsc 干净；schedule.test.ts 已入 npm test 链。**已知限制**：terminal 套件 node-pty AttachConsole 崩溃 = 无交互控制台环境噪声（结果行后出现，非回归）
- ✅ **v3.6 全库审计修复批（2026-08-18，第三方独立审计 7.3/10 后逐项修复）**：**① 安全**——新增 `@infu/shared/net.ts`（IPv6 完整解包 + IPv4 简写归一化，`isPrivateIp`/`isLoopbackTarget` 共用）修复 `::ffff:7f00:1`/`::7f00:1`/完整 `::1` 变体绕过（此前可直达带 token 的 InFu 服务自我提权）；程序化导航（browser-view:navigate + 地址栏）统一走 sanitizeBrowserUrl；deleteAgentFile 补名称正则；附件 rawPaths 过滤受保护路径；sanitizeEnv 补 `_PWD`/PROXY。**② 数据完整性**——appendEvent 改 INSERT...RETURNING 原子分配 seq；mcp/plugin register readConfig 损坏抛错（原空配置覆盖冲掉用户凭据）；memory replace/desktop-window 原子写。**③ 生命周期**——runAgent finally 按深度清理后台子 Agent/job（孤儿修复）；scheduler-runner 补清理；persistent-shell 512KB 环形裁剪；fetchText 流式防 OOM；dockerAvailable unref；subagent/chat 监听器移除；死代码清理（createModel/throw lastError）。**④ 跨会话**——todoStores 按会话键控 + clearTodos；pluginSkillDirs 任务清理。**⑤ 测试**——9 套件数据目录重定向（approval-policy 曾把用户档位写成 full 的前科根治）；4 处恒真断言修复；**新增 net.test.ts（IPv6 变体矩阵）+ server-api.test.ts（bypass 路由/会话删除联动/令牌 401）**，npm test 40→42。**⑥ 前端**——finishTool 全消息匹配（工具行不再卡运行中）；审批/提问失败重新入队+提示（不再静默挂起任务）；TerminalPanel 退避；routeSubagentEvent 不可变；CodeView 竞态守卫；死代码清理（browserOpenTick/setUsage/fetchTemplates/discardWorktree/StateDot/DisclosureRow）。**⑦ 桌面/Rust**——powerSaveBlocker stopped/error 解除；token.rs 句柄泄漏修复；process.rs 管道 EOF 死锁修复（recv_timeout+读端强关防 double-close）；README 全面同步。**验证**：agent/web/desktop tsc + cargo check 全过（测试运行受本环境 EPERM 限制）；**已知限制**：runRestricted 无 abort 通道（结构性改动风险高，timeout 兜底）、browser_eval low 免审批（设计权衡）

- ✅ **v3.5 审计修复批 2（2026-08-17，用户四项任务：full 档图标 / 卡死诊断 / 自适应尺寸 / 自由修复审计）**：**① full 档图标收尾**——SettingsModal MODE_META 四档图标（auto=ShieldAlert 警告色、smart=Scale 中性、confirm=ShieldCheck 中性、full=AlertTriangle 红）+ save 同步 store；**CLI makeDecider 补 full 档分支**（shouldAutoApprove 先行 → 红线不再拦截，与 Web full 档一致，此前 CLI 缺 full 语义）。**② 卡死诊断（会话 3e717b18 实证）**——截图成功返回后**模型调用 52 秒零输出**挂起，根因 = chat.ts 无首字节超时 → **两段式预算**（首帧前 min(timeoutMs,60s)/首帧后 timeoutMs + 区分「等待响应超时」/「响应中断」文案），retry 套件兼容（timeoutMs<60s 行为不变）。**③ 自适应尺寸修复**——根因 CDP Emulation 残留（用户点击只改 CSS 不改视口）：渲染进程→主进程 IPC `browser-view:set-viewport`（Emulation.setDeviceMetricsOverride/clear + __infuNotifyViewport），预设/适应/自定义三处接线。**④ 审计修复**——安全：dangerous 补 Remove-Item、net-policy 补 iwr/irm/fetch/Invoke-WebRequest、git_status/git_diff/run_test 补 isPathInside、**桌面 H1 改仅拦 InFu 自身端口**（v3.4 拦全部 loopback 误伤 localhost:5173 本地预览）+ will-navigate/will-redirect 守卫 + setWindowOpenHandler deny + CDP 桥 Page.navigate 兜底、令牌注入窄化 index.html；逻辑：loop 压缩闸死代码（`if(r.summary)` 永不触发 → `if(r.after<r.before)`）、cron 周日 0/7 双接受、**server H4 挂起队列带 sessionId 按会话清理**（并行会话不再互相误清）、MCP callTool 180s 超时 + unref、db WAL+busy_timeout、agents.ts 名称白名单、终端 data 字段逐行推导高危/审计；前端：**审批跨会话串扰**（store resolve* 按 activeSessionId 过滤 + ApprovalModal 只处理本会话）+ **QueueDock Stop&Send 竞态**（abortRun 后轮询 runningIds 再 sendChat，8s 兜底）；测试基建：npm test 补 4 套件（cleanup/data-dir/refine/vision）、**memory/cleanup 测试数据目录重定向**（原写真实 ~/.infu）、approval-cache/web-tools 文件级固定 smart 档（备份/恢复——原隐式依赖用户 config 档位）、假阳性断言清理 4 处。**验证**：**40 套件全量单次跑完 0 失败**（含 memory 84/cleanup 19/data-dir 31/refine 13/vision 25/approval-cache 25/web-tools 10 等）+ agent/web/desktop tsc + vite build 全过（修 3 编译错：server.ts lambda 隐式 any、SettingsModal JSX 计算组件、main.ts notifyViewport 可调用性）；用户真实 config 已确认恢复 `mode:"confirm"`（此前测试 full 档遗留）。**注意**：approval-cache/web-tools 测试会短暂改写 ~/.infu/config.json（自动恢复）

- ✅ **v3.5 补批：四项用户反馈修复 + 数据目录可迁移（2026-08-17，用户四项反馈 + 拍板方案 B「不怕改得多」）**：**① 审批「本会话全部放行」按钮修复（根因实锤）**——server.ts 路由顺序 bug（`POST /api/approvals/bypass` 注册在 `/api/approvals/:id` 之后，"bypass" 被 :id 吞掉返回「审批不存在或已过期」）→ 前移；配套 store.ts ApprovalState 加 sessionId、api.ts 两处传 connSid、ApprovalModal 用 approval.sessionId + addError（不再静默）。**② 数据目录可迁移（对齐 ZCode「根目录可选、内部结构固定」）**——新 `src/data-dir.ts`：`~/.infu-redirect.json` 固定指针 + resolveDataDir 进程级缓存 + migrateDataDir（**复制迁移**旧目录保留备份；校验绝对路径/非当前/非主目录/非盘根/非嵌套/空目标）；全部用户级路径惰性函数化（configPath/logDir/logFile/projectsFilePath/schedPath/commandLogPath/defaultDbPath/index/agents/skills/globalMemoryDir/attachments/plugins/desktop-window.json/isProtectedPath 动态 dataDir）；API `GET/POST /api/data-dir`；设置「数据与统计 → 数据存储」Tab（DataDirPane：路径显示 + 桌面 selectPaths / Web 手输 + 两段式确认 + 非空拒绝回显）。**③ 命令 Tab「全权放行」红点常亮**——SettingsModal 圆点仅 full 才 bg-danger。**④ read-before-edit 改 opencode 语义**——删 write/edit「未读拒绝」硬闸改建议性描述 + old_text 提示增强。**验证**：新增 data-dir 套件 31/0；全量套件 0 失败（tools 23/projects 21/memory 84/cleanup 19/session-store 30/approval-cache 25/approval-policy 61/sandbox-config/win-sandbox/mcp 13/plugin 78/subagent 94/subagent-control 24/jobs 19/v212 16/git-tools 17/fs-tools 37/web-tools 10/task-tools 13/builtin-skills 11/vision 25/win-sandbox-net 1 等）；真实迁移端到端（复制齐全 + 指针 + 重启恢复）；smoke6/7 全过；agent/web/desktop tsc + vite build 全过
- ✅ **v3.5 常规设置 10 项 + 审批 full 档 + 数据生命周期治理（2026-08-17，用户七项任务）**：**① 审批 full 档**——shared ApprovalMode 加 `full`（「全权放行」红色卡片）：所有审批自动放行（含 requireExplicit 红线），仅剩硬闸（显式禁用工具/受保护路径/断网策略/路径作用域）；CLI `-y` 跳过提问、scheduler 不传 askUser；审计照常。**② 设置有效性审计**——GET /api/config 补 memory 节、shared 删 capabilities 死代码、web 删 fetchRoles/saveRoles 孤儿、desktop 启动复算 autoLaunch。**③ 数据全生命周期**——日志轮转（sandbox maybeRotateLog 5MB×3 份 + agent.log 同式）；会话删除联动清理（outputs/browser 会话前缀文件 + attachments 目录）；removeProject 同步 deleteIndex 防索引孤儿；cleanupOldBackups 7 天备份清理（projects/schedule 损坏备份接入）；orchestrator 收尾 discardCleanWorktrees（干净 worktree 移除 + 分支删除）。**④ autoCommit（默认关）**——任务收尾 git add -A + commit（指令前 50 字，**绝不 push**），交付文本追加提示，静默跳过非仓库/无改动/无身份。**⑤ autoRefine（memory.autoRefine 默认开）**——新 memory/refine.ts：Executor 模型单次轻量调用提炼 conventions/lessons/preferences JSON → writeMemory 追加，失败静默。**⑥ 常规设置 10 项 UI + 接线**——taskNotifications/notificationSound/autoContinueQuestions/showThinking/showTodos/autoArchive + archiveRetentionDays + closeToTray/preventSleep/数据路径只读说明；**ask_user 语义定稿**（autoContinueQuestions 开=5 分钟未答自动继续、关/默认=一直等——替换 v3.4 的 15 分钟兜底）；server 启动 autoArchive（超保留期自动归档）；桌面 main.ts：closeToTray 拦截 hide + before-quit 放行、powerSaveBlocker 防休眠（user-message 启/done 停）、任务完成 Electron Notification（silent 随开关）。**⑦ 对话流收尾**——滚动跟随阈值 48→24px。**验证**：agent 全量套件绿（新增 cleanup 19 + refine 13；settings-api 62/approval-policy 61/approval-cache 25/vision 25/task-notify 31 等全 0 失败）+ web tsc/vite build + desktop tsc 全过；terminal AttachConsole 仍为环境限制
- ✅ **v3.4 审计修复批（2026-08-17，综合评分 7.6/10：安全 7.5 / 核心循环 7.0 / 工具 7.5 / 前端 8.5 / 桌面 8.0 / 测试 7.5 / 文档 7.0）**：**H1 回归测试（vision）**——新 `tests/vision.test.ts` 25 项（read_image 注入/边界/越界/8MB、screen_capture 注入 + H1 浅拷贝回归、screen_click 审批流、screen_type/scroll/key/move/drag/windows 通道、非桌面拒绝）。**安全修复**——M7 screen_capture 8MB 上限（statSync 拒绝不注入）；M8 web.ts 八进制 IP SSRF（前导零 fail-closed）；M9 isProtectedPath win32 大小写归一（防 `.INFU` 变体绕过）+ sanitizeEnv 敏感词补 URL/URI/DSN/CONNECTION；M1 桌面 sanitizeBrowserUrl 加 isLoopbackTarget 拦截（webview sandbox=no 防读本机服务，IPv4 简写首段 127/0、非标准数字段 fail-closed）；M12 gitRun isPathInside 越界拦截。**健壮性**——M5 lsp 惰性 tsserver（不可用空结果不再崩溃）；M6 subagent trimBackgroundAgents / jobs trimJobRegistry（完成保留 20 剪枝）；M3 MCP/插件工具与内置重名兜底改名 `ext_` 前缀（loadMcpTools/loadPlugins 预置内置名）；M4 server/scheduler-runner finally 统一三项清理（clearObservedFiles/clearApprovalMemory/clearSessionBypass）+ pendingApprovals/pendingQuestions/pendingPlans 任务结束清空。**低危批量（后端）**——delegate_task 写委派 requireExplicit=true；wait_task 检查 abortSignal；run_command 输出落盘前凭据检测（命中不入盘裁剪回填+警告）；saveConfig chmod 0600（非 win32）；schedule 加载逐条校验+损坏备份、cronMatches 删死参数；ask_user 15 分钟超时；`/api/sessions/:id/events` 白名单 12 类；loop 摘要调用补 usage 聚合 + model-call 事件（shared 加 summary 字段）+ rawToolCalls→validToolCalls 统一回填 + max-steps 总结失败降级完成不再 throw；dangerous.ts 三个 RegExp 按位或编译错 → 合并单正则；web.ts 工具头注释修正（low+netGuard）。**低危批量（前端）**——apiUrl() helper + server token 中间件接受 `?token=` query（img 无法带 header；本机随机 token 随进程重建）；ComputerUsePane 截图 img 改 apiUrl + 缩略图失败占位（此前桌面打包版截图预览全部 401）；ReviewPane diff 竞态守卫（diffSeq 序号作废过期响应）；Modal 焦点管理 + 背景滚动锁（Tab 不逃逸、wheel 不穿透、多层嵌套自然恢复）+ Esc 注释修正；ChatPanel 三处静默 catch → addError（插件列表/审批档位/项目列表）。**验证**：agent 34 套件全绿（vision 25 新增、win-sandbox 38、win-sandbox-net 1）+ web tsc + vite build + desktop tsc 全过；terminal 套件 node-pty AttachConsole 失败 = 无交互控制台环境限制（检测/审计断言全绿，非回归）；browser 平台门控跳过
- ✅ **v3.3 异步任务编排 + computer use 补齐（2026-08-17，用户三项任务：调查 ZCode 等待机制 / 核实 opencode 改动 / 审计 computer use）**：**① ZCode「等待时先做别的」机制原理（asar 实证 + 两次真实通知注入）**——四件套：异步启动（Agent/Bash `run_in_background` 立即返回）→ **完成通知注入**（后台任务完成/失败/停止时向会话注入 role=user 的 `<task-notification>`/`<subagent-notification>` XML：task-id/tool-use-id/output-file/status(completed|failed|stopped|killed|lost)/summary——模型下一轮请求实时感知，自主决定回收结果或继续别的）→ TaskOutput block=true（阻塞等待）/ block=false（非阻塞查询）→ TaskStop/SendMessage 生命周期。**InFu 落地（补齐缺失四件）**：① **完成通知注入**——新 `task-notification` 事件（shared AgentEvent）+ runAgent 局部队列 `pendingNotes`（ToolContext.enqueueTaskNotification 接线）每步开始 drain → user XML 消息注入 messages（与 rebuild.ts 同格式恢复，纯文本不破坏 assistant/tool 配对）；subagent（startBackgroundSubagent 完成/异常两路径）与 job（startBackgroundJob finishOnce）完成点 emit + 入队；② **wait_task 阻塞等待工具**（第 50 个，ZCode TaskOutput block=true 语义：轮询 500ms 等待 subagent/job 完成返回结果，超时返回进度让模型决策继续等或先做别的；与 report/job_output（非阻塞查询）互补；进 READONLY_TOOLS）；③ **提示词「异步任务纪律」3 条**（耗时任务优先 background 异步启动→先做别的；收到 <task-notification> 后回收/驱动/中止；需要结果才 wait_task，不空转轮询）；④ **前端 EventRow 通知行**（store taskNotes + ChatPanel：CheckCircle2 绿/XCircle 红/OctagonX 黄/Skull 红按状态 + 展开摘要，subagent 提示可看右栏 tab）。**② opencode 改动核实 = 13/13 全部落实并在 UI 显现**（前端逐项核查：审批全权放行按钮+⚡徽标、EventRow 事件行、失败摘要顶替、错误类型徽标、重试倒计时、三栏 3.25rem、批量审批、热力图/趋势图、ComputerUsePane、SchedulePane；tsc + vite build 全过；仅 RightRail 过时注释与 CU 图标两处小瑕疵顺手修）。**③ computer use 审计（联网对照 Codex 官方契约 get_app_state/click/scroll/drag/press_key/type_text/window 管理 与 Claude Code 截图/单击双击三击/拖拽/打字/修饰键）**——结论：**核心闭环（截图→视觉→点击/输入/滚动/按键/移动 + DPI 多显示器修复 + 审批控制）已达主流水准，但 opencode 声称的「Codex/Claude Code 最新水准」不完全属实**——缺三项：拖拽 drag、窗口/应用管理（list/activate）、UI 结构读取（accessibility tree）；**用户拍板补 drag + 窗口管理**：screen_drag（x1y1→x2y2 分步拖拽，SendInput down/move/up，medium 审批）+ screen_windows（list 可见窗口 low / activate 前台激活 medium；PowerShell Get-Process+MainWindowHandle+SetForegroundWindow+ShowWindow，**Contains 模糊匹配防正则注入**，截图坐标系原点偏移回补）。工具 49→52；Timeline/ComputerUsePane 图标补齐。**验证**：新 task-notify 套件 31 项（job 三态通知/subagent 通知/运行时注入 XML 转义/rebuild 注入/wait_task 完成·超时·未找到/不破坏配对）+ 全量 37 套件无回归（subagent 只读断言 14→15 同步）+ agent/web/desktop tsc + vite build 全过 + CLI 真实 agnes 端到端（后台长命令→等待期读文件→收通知→wait_task 回收→回复含 E2E_JOB_DONE）。**遗留**：UI 树读取（accessibility tree，对齐 Codex get_app_state）未做（高成本，拆分后续）
- ✅ **v3.2 补 2：审批全权放行 / read-before-edit / computer use 增强 / description 修正（2026-08-17，用户四问后拍板）**：**审批流畅化（用户拍板方案）**——审批弹窗新增「**本会话全部放行**」按钮（ApprovalModal + `/api/approvals/bypass` 端点 + `approval/cache.ts` sessionBypass + guard 插点）：开启后本会话内所有审批（**含联网/自注册/高危命令红线 requireExplicit**）直接放行，直到会话结束；**显式禁用工具仍拒绝**（对齐 opencode 显式 deny 不覆盖）；CLI `-y`/定时任务无人值守不受影响（没人点按钮，红线拒绝语义不变）；运行状态行「⚡ 本会话已全权放行 · 点击关闭」徽标可随时关闭；会话删除/任务结束清理（server/cli finally）。**调研结论**：harness 审批不完整（无命令分析/无批量/无记忆，danger-full-access=approval never 粗放零弹窗）；opencode 完整参考（--auto 除显式 deny 外全放、Ask 弹窗 once/always/reject——always=会话级记忆与 InFu 已批准记忆同思路）；InFu 现为三者中最平衡。**read-before-edit（对齐 harness fs-observation-policy / opencode Edit 先读后改）**——tools/index.ts 按会话跟踪已读文件（Map<sessionId, Set<abs>>，win32 归一化）：`edit_file` 未读拒绝（"编辑前必须先 read_file 该文件"）、`write_file` 覆盖已存在文件未读拒绝、新建免读、read/edit/write 成功都刷新观察（本会话刚写过算已知，对齐 harness fs/observed 语义）；工具描述更新引导模型；clearObservedFiles 挂 server/cli 会话清理。**computer use 增强（对齐 Codex/Claude Code 工具集）**——修复 **DPI 缩放点击坐标偏移 bug**（PS 进程 DPI 非感知时 CopyFromScreen 返回逻辑分辨率位图而 SetCursorPos 用物理像素 → 125%/150% 缩放系统性点错；修 = SetProcessDPIAware + VirtualScreen 全显示器合并 + 原点偏移回补 lastShotOrigin）+ 新增 **screen_scroll**（滚轮方向 up/down/left/right+格数）/ **screen_key**（组合键 ctrl+c/alt+tab/f5/方向键等全 VK 码表）/ **screen_move**（悬停定位）三工具（46→49，scroll/key medium 审批、move low）。**description 修正**——package.json 乱码 "InFu - 软件工程智能体平台（Infinite Future�?" → "InFu"。**验证**：agent/web build 全过；approval-cache 25（新增 bypass 8 断言：开关/会话隔离/红线放行/禁用仍拒/关闭恢复）、tools 23（新增 read-before-edit 9 断言）、memory 84 全绿；全量套件无回归。commit `0270a6d`
- ✅ **v3.2 消息流对齐 harness + token 优化 + 断网可见性（2026-08-17，用户「抄作业+差异化」四项任务）**：**UI（Electron 原生融合）**——三栏顶部统一 `3.25rem`（52px@16px 随字号缩放；聊天 header + Sidebar Logo 行 `calc(3.25rem+1px)` + 折叠 rail 让位区 `calc(3.25rem-11px)+py-3(12px)` = 精确同 y；CodeView top 同步）、折叠 rail 按钮文字→PanelRightOpen 图标圆钮、RightRail 活动 tab 顶部 2px 信息蓝指示条、RightRailEmpty 空态优化（主按钮独立区+分隔线+次按钮 hover 上浮）、composer 描边 `border-line/60`；**消息流对齐 harness（差异化）**——模型降级/上下文压缩事件升级为 **EventRow 可展开折叠行**（降级=warn AlertTriangle、压缩=info Files 显示摘要全文）、失败工具行错误首行顶替参数摘要（+hover:bg-danger-soft/50）、错误行类型徽标（classifyError：网络/超时/限流 429/认证/流中断/HTTP n）、运行状态行重试倒计时（WifiOff +「网络错误，正在重试 1/3（2 秒后）」）；**token 优化（借鉴 harness compaction）**——① `balanceToolPairs` 压缩边界工具对平衡（kept 首条为 tool 结果时回溯配对 assistant 不跨 user 边界，防孤立 call_id API 400）、② `SUMMARY_MUST_BE_SMALLER` 摘要过大拒绝（摘要估算 ≥ 被替换内容 → 降级直接丢弃，防压缩后更占）、③ 400 上下文超限自动压缩重试（`isContextWindowExceeded` + loop catch `ensureContextBudget(true)` 强制压缩后清空累加器重试一次/轮）、④ usage miss 兜底（miss = max(0, prompt - hit)，命中率不再虚高）、⑤ 前端中英混合估算（中文 1:1 其他 4:1 与后端同式）、⑥ 空闲超时 120s→300s（对齐 harness，深度思考长思考不再误杀）；**断网可见性**——streamChat `onRetry` 回调（attempt/maxAttempts/delayMs/message）→ gateway 透传 → loop emit `retry` 事件 → SSE → 前端状态行倒计时 + 落库审计。**验证**：agent/web tsc + vite build 全过；全量套件分批跑绿（compress 33 新增摘要拒绝/配对 5 断言、retry 22 新增 onRetry/超限识别 8 断言、其余 30 套件无回归；browser 套件平台 kill 跳过——v3.1 已绿且本次无关）
- ✅ **v3.1 复核批 1：高危修复 + 审批流畅化 + 工具补齐（2026-08-17，用户「自由发挥全面优化」；基于全库代码级审计 ≈7.5/10）**：**安全**——本地令牌鉴权（staticDir 时 token 注入 index.html + X-InFu-Token 校验 /api/*，防恶意网页操纵本机 API）、gitRun 改 execFile 数组直传（cmd.exe 注入根治）、DANGEROUS 模块级常量 + run_command 高危 requireExplicit（无人值守不再放行 rm -rf）+ run_test 自定义命令同门槛收口（删 low 旁路）、MCP stdio env 以 sanitizeEnv 为基底、SSRF 简写归一（127.1/32 位十进制/::ffff:/十六进制 fail-closed）、net-policy 补 certutil/bitsadmin/mshta/regsvr32/nslookup 等、嵌入式浏览器 file:// 封堵（normalizeUrl 空返 + 主进程 sanitizeBrowserUrl）、打包排除 .infu-worktrees；**逻辑**——usage 双计修复（chat.ts 末尾快照单次 yield + loop 成功轮统一并入）、max-steps 收尾前 ensureContextBudget、cron 周日 7/语法校验/调度防重入、persistent-shell 监听器泄漏 + 持久分支补审计；**审批流畅化（用户核心诉求）**——会话级「已批准记忆」（非红线同会话同参批准一次即放行，256 条 FIFO，requireExplicit 永不记忆）+ 审批弹窗批量「全部允许/全部拒绝」；**工具补齐对齐 opencode**——project_tree（只读白名单）/ file_ops（mv/cp/rm/mkdir 三检）/ os_info / current_time（工具 42→46），Timeline 图标补齐，system prompt 加工具纪律 5 条（探索先树、文件操作用 file_ops、read 带行号、run_test 优先、一次一状态改变）。**验证**：37 套件全绿（新增 approval-cache 17 + fs-tools 37，subagent 断言同步 11→14）、agent/web tsc + vite build 全过、agent dist 重建
- ✅ **v3.5 审计后收尾（2026-08-17，opencode 优化后我全量验证 + 修复 7 处）**：**① read-before-edit 升级为 ZCode 式三层门禁（2026-08-17 晚用户实证纠错）**——用户指出「我被 ZCode 拦了」（File has not been read yet 报错）→ 重查发现 ZCode CLI 工具宿主在 `~/.zcode/cli/plugins/cache/zcode-plugins-official/browser-use/*/dist/mcp/server.js`（@zcode/browser-use-plugin = 官方内置插件承载全部核心工具 Read/Write/Edit/Bash/Glob/Grep/Agent/TodoRead/TodoWrite/Skill/js，fs 工具来自 @zcode/core 的 tool/handlers——**桌面版 D:\app\ZCode asar 里没有**，之前查错对象）；机制三层：① 未读拒绝（含 truncatedByTokenCap 截断视图 isPartialView 拒绝——offset/limit 范围读允许但截断视图拒绝）② **stale 检测**（readFileState 记录 revisionId+mtimeMs+sizeBytes 指纹，写时比对——读后外部修改 → WRITE_STALE_MESSAGE 拒绝；full read 才做 content 级比对）③ 写后更新（updateReadFileStateAfterWrite 新指纹 + atomic+expectedRevision 文件系统层乐观锁）。InFu 已升级（tools/index.ts）：Set 布尔 → Map<abs, {content, offset, limit, isPartialView, mtimeMs, sizeBytes}> + assertFreshForWrite 三层校验（未读/partial/stale 拒绝文案）+ read 记录指纹（clip 截断 = partial）+ write/edit 写后刷新指纹 + 新建免读；工具描述同步；tools.test.ts 断言 14 项全绿（未读拒绝/已读放行/stale 拒绝/重读恢复/写后续改/partial 拒绝/新建免读）。**② 审批档位 4 档（auto/smart/confirm/full）落地确认**——composer 下拉 + 设置界面均 4 档，full（全权放行）红色 text-danger + 设置选中红点（opencode 已做）。**③ opencode 测试遗漏修复**——approval-policy/settings-api 测试 import 已删除的 CONFIG_PATH → configPath()（此前测试直接被 SyntaxError 中断只跑 1 个套件）；DANGEROUS 正则命中片段截断（rm -rf 返回 rm -r，贪婪回溯）→ 非贪婪 + 连续标志组 -r[fv]*|-f[rv]*|/[sqfr]+，19 用例全过。**④ data-dir 迁移 3 个严重 bug**（opencode 新增的数据目录迁移功能实测抓出）：相对路径被放行真实迁移（resolve() 先行使 isAbsolute 永远 true——差点把 ~/.infu 迁进项目目录，现场已恢复）；服务进程内整体 fs.cpSync abort（0xC0000409，独立进程/逐项复制均正常——改逐项复制规避）；迁回被「目标非空」拒绝（原目录含 config.json = InFu 数据目录应放行互迁）+ **迁移后 DB 连接不跟随**（SessionStore 单例持旧库连接，新增 resetStore() 迁移成功重连，实测新库 mtime 变化 ✓）。**⑤ projects.json 反复损坏根因 = 非原子写**——多进程并发（server/CLI/定时任务）writeFileSync 截断半写 → 读方 JSON.parse 失败 → 累积 9+ 个 .corrupt-* 备份；三处（projects/schedule/config.json）统一改原子写（tmp + rename，config 保留 0600 chmod）；已删全部 corrupt 备份 + 实测 10 次增删项目 0 新增。验证：全量测试全绿 + 迁移端到端（迁出→写新库→迁回）闭环。**遗留**：~/.infu 已有 1 个旧项目「InFu(test)」（E:\InFu(test) 目录可能已删，用户自决）
- ✅ **v3.0 审计后批 1：统计页改 ZCode 同款 + 侧栏分隔线对齐（2026-08-16，用户 opencode 审计 8.2 分后提出）**：**统计页**——解析 ZCode asar（D:\app\ZCode\resources\app.asar，**头部 16 字节三层 pickle + jsonSize@u32(12) + baseOffset=16+jsonSize**，文件 offset 均为绝对偏移）提取 usage 组件源码（usageStatsUiParts/AppUsageDailyModelBarChart/appUsageChartPalette）为基准重写：① **活跃热力图 = 真 GitHub 贡献图式**（推翻批 2 的横向星期版）：横向周列（时间从左到右）、纵向周一~周日 7 行 + 左列星期标签、色阶图例在**标题行右侧**横排「少 → 5 级色块 → 多」（0.15/0.35/0.55/0.75/0.95）、hover scale-110 + title（日期+tokens）、数据多时 overflow-x-auto + 自动滚动到最新（ZCode 53 周固定，InFu 按 days 范围自适应列数）；② **按天 Token 趋势 = 竖向堆叠柱状图**（ZCode 同款，Recharts 语义用纯 div 实现）：柱高 = 当日总量/maxDayTotal、柱内按模型分段着色、Y 轴隐藏 + 水平虚线网格 5 档（0/25/50/75/100）、底部日期刻度稀疏显示（≤14 全显、>14 每 5 个 + 首尾）、图例在图表下方 **3 列网格**（size-2.5 色块 + 模型名 truncate，最多 6 模型）、估算单条 accent/70；③ 统计卡片加图标（ZCode Ne 同款：Coins/MessagesSquare/MessageSquare/CalendarCheck/Flame/Sparkles）。**豆包描述与 ZCode 实测不符处一律按 ZCode**（豆包说「横向星期、纵向月份」实为反的；「横向条形图」实为竖向堆叠柱——用户明确「豆包不一定对」）。**侧栏分隔线对齐**：Logo 行 60px → `h-[calc(2.5rem+1px)]` + `border-b border-line`——聊天 header 高 2.5rem（14px 根字号下 35px）+ 卡片顶部 1px border → 两条线精确同 y（实测 36=36），随字号缩放自适应（h-10 硬编码会随根字号漂移 1px 差）。**验证**：web tsc + vite build 全过；playwright 实测（设置→使用统计：标题/副标题/色阶少多/星期 7/矩阵着色/柱状图 5 网格线/图例/刻度全渲染、热力图 5 级色阶 5 span、两条分隔线 y 相等、零 console 错误）。**遗留**：用户可能要求趋势图改回横向条形（豆包版），未做；2026-08-16 用户口述 + 两轮实测反馈定稿（用户看到的必须是桌面窗口实际渲染——**改 src 后必须 vite build + 重启桌面实例**，桌面版加载 packages/web/dist，5174 vite dev 与桌面无关；CDP 验证 = `--remote-debugging-port=9222` 启动 + playwright connectOverCDP 查主窗口 DOM）：① **热力图固定最近 30 天**（不受右上角 7/30 切换控制，实测切 7 天仍 5 列）；② **最新日期在最右端**（时间从左到右 = 旧 → 新——用户口述「最左边是最新」为口误，实测反馈更正「按理说 8/16 应该在最右端」）；③ 趋势图受 7/30 切换控制，**柱宽随天数自适应**（≤14 天 gap-2 粗柱、>14 gap-px 细柱，flex-1 + **max-w-[56px]**——无 max-w 时单根柱占满整卡 = 「整个卡片都是绿色」bug），**容器 justify-end**（单根柱靠右贴卡片右端，8/16 在右端）；④ 日期刻度 ≤14 全显、>14 每 5 天一组 + 首尾（8/1~8/5 一组语义）；⑤ 星期标签竖排卡片最左端（左对齐，勿居中）；⑥ **空格子必须可见**（border-line/60 + bg-muted/40 底——透明底时 30 天只 1 天有数据 → 用户看到「没有矩阵只有 1 个绿格」）；**⑦ 三修（2026-08-16 晚用户实测反馈）**：热力图 = **30 列 × 7 行矩阵铺满卡片**（列 = 天，旧→新 8/16 最右；行 = 星期；当天格 = 数据格，同列其余 6 格 = bg-muted/15 浅底占位——不是周列 5 列版！gap-[2px] 让格子 18px 铺满 97% 卡片宽；**占位格也必须有淡边框（border-line/40 + bg-muted/15）——210 格全可见，用户明确「其他矩阵里的方格没有」**）；趋势图 = **日期轴 7 天全显示 / 30 天每 5 天一个（(days-1-i)%5===0 从今天往回数，今天必有：8/16、8/11、8/6、8/1…）** + **槽位制柱宽**（柱区/日期轴 = days 个 flex-1 槽位对齐，有数据的天在对应槽位出柱——30 天模式单日数据也是细柱 ~19px；7 天模式粗柱；无数据槽位空 div 占位）
- ✅ **v3.0 UI 审查批 2：三需求（2026-08-16，用户拍板）**：**① 代码/审查按 root 可用性**——自由会话（root 为空）不再显示启动目录所有原文件：App「代码」按钮 disabled + title 提示；CodeView/ReviewPane root 为空时空态（修此前永久「加载中」bug）；**后端配套**：server.ts 会话落库 root 只写显式值（隐式回退 defaultRoot/cwd 不写回会话——前端 root 保持空，禁用逻辑稳定）。项目会话/配了默认目录的会话照常可用。**② 折叠 rail 分隔线**——右侧栏折叠 56px 态在 38px 原生窗口按钮让位区与「Tab」按钮之间加 `border-b border-line` 分隔线（与聊天 header 同款）。**③ 统计页双图表**——新 `model-call` 事件（loop 每轮成功调用 emit：时间/模型/prompt/completion/cache 四桶，子 Agent 自动带 subagentId 归属；失败重试轮用 stepUsage 局部累加器不污染）；getStats 重写：modelUsage 真实聚合（无 model-call 回退 phase-start 近似）、dailyTrend 日期集合 = done ∪ model-call（纯 model-call 会话也出趋势）+ 每项 byModel 按天×模型；StatsPane 重写为**上下布局**：上「活跃热力图」、下「按天 Token 趋势」**横向条形图**（Y 轴日期自上而下、X 轴 Token 量、同日多模型并列条按模型色板区分、底部模型图例；旧数据单条估算），保留 6 统计卡片 + 模型用量列表。**用户反馈修正**：热力图改 GitHub 贡献热力图式——横向星期（一~日 7 列固定）、纵向周行 + 行首月份标签（跨月行显示）、左侧色阶图例（高→低竖排）、格子 aspect-square 随窗口缩放自适应（max-w 440px 居中）；条形图标题改「按天 Token 趋势（模型色标区分）」；折叠 rail 分隔线让位区 38→28px（+py-3 12px = 40px，与聊天 header 底部线水平对齐，实测 y=39/40）。**验证**：web/agent tsc + vite build 全过；settings-api 套件绿；model-call 聚合单测 PASS（modelUsage 150/50 share 75/25、dailyTrend 纯 model-call 日 tokens=200 estimated=false byModel=2）；playwright 冒烟（代码按钮 disabled+title、折叠 rail 分隔线、热力图/条形图标题与数据格渲染、零 console 错误）
- ✅ **v3.0 UI 审查批（2026-08-16，用户拍板「全做吧」；17 条 UI + 4 条对话流 = 21 项全落地）**：**对话流 A/B（消息 memo 化）**——ChatPanel 消息 map 抽为 `MessageItem`（React.memo：流式 appendText 每帧只替换最后一条消息对象，旧消息引用不变 → 历史消息不再每帧重渲染/重解析 markdown/重建工具行）；lastEditIdx/lastUserIdx/rmIdx 组件级 useMemo 预计算（消除原每消息 O(n) reduce）；askRewind/startEdit/doMerge useCallback 化（askRewind 改 getState 读数据，不依赖流式闭包）；Streamdown 已完成消息改 `mode="static"`（省不完整解析开销，parseIncompleteMarkdown 仅流式）；**其余 17 条 UI**——#2/#3 ReviewPane（fetchReviewFiles catch 空态 + pickFile try/finally 失败提示、run_test 状态标签 运行中/失败/通过）、#4 Timeline 子 Agent 点击追加 setDetailsOpen(true)、#5/#16 BrowserPanel（pending 未就绪 tab 关闭跳过主进程防 guest 残留；删 visible 死参数）、#6 SettingsModal（字号/主题点击即时生效 setAppearance + 文案统一「点击即时生效，保存后持久化」）、#9/#10 SettingsPanes（SchedulePane 删 window.confirm 改行内两段式确认；6 处手写 Toggle 收拢共享 Toggle 组件 + SettingsModal autoLaunch 同款）、#12 ComputerUsePane（大图 × 按钮 + Esc 关闭）、#13 SettingsTab 类型（store 导出字面量联合，Sidebar onOpenSettings 类型同步收紧，App 删 cast）、#17 App 代码模式隐藏右栏拖拽热区、#7/#15 ModelPane（删 preset 死代码；setDefault 成功后 setModelId 同步）、#8 删除 TitleBar.tsx、#1/#11 ChatPanel（编辑确认后清空输入框、删空模板 useEffect）。**D 条（错误行 ⚠️ 前缀判定）按评审建议不实施**（判定稳定无 bug，不值得改）。**验证**：web tsc + vite build 全过；playwright 真实会话冒烟（agnes 回复渲染/编辑按钮=1/回滚=1/复制=2/统计行/零 console 错误）
- ✅ **v3.0 审计修复批（2026-08-16，用户授权自由优化；审计评分 8.2/10，12 项全落地）**：**S1 CORS 白名单**（server.ts：Origin/Host 校验，仅 localhost/127.0.0.1/[::1]，非白名单 403 防 DNS rebinding）；**S2 Reviewer run_test 任意命令**（getReviewerTools() 包装 run_test 拒绝 command 参数——"只读审查"不可再当任意命令执行器）；**S3 持久 shell 凭据消毒 + 清理**（spawn env 改 sanitizeEnv——模型不可 echo 读 INFU_*_API_KEY；closeShellSession 挂 server/cli/scheduler-runner 任务结束 finally，此前永不清理泄漏常驻进程）；**S4 命令白名单收紧**（`npm run*` 移除——执行任意 package.json 脚本不再免审批，仅保留无参 `npm run` 列脚本；bugfix 测试断言同步更新）；**S5 webfetch 重定向 SSRF**（fetchText 改 manual 逐跳跟踪 + 每跳 isPrivateTarget 复查——防 https://公网 → 127.0.0.1/云元数据 绕过；文件头过时门禁文档修正）；**S6 symlink 逃逸写保护**（isPathInside 双检 = 词法在内 + realpath 解析后仍在内，junction 指向外部拦截）；**S7 前端裸 fetch 收敛**（apiFetch 导出，store + 8 组件改道——审批/ask/截图流/终端流/归档/建项目/解析/定时任务/模型管理，桌面 dev 端口 query 全生效）；**B1 重复调用提醒条件反转修复**（原 `ok &&` 成功才提醒、失败死循环不提醒 → 「第 N 次且上次失败」即提醒，与批 6.5 注释意图一致）；**B2 计划修订超限静默降级**（第 3 轮 revise 明示用户 + 意见并入附加指示，不再无声落 execute）；**B3 chat 超时改空闲超时**（收到数据帧重置计时——长输出/思考链不再被总时长误杀，服务端挂起仍中止）；**C1 桌面截图 PS 路径注入**（单引号 `''` 转义补全）；**D1 工具参数运行时 schema 校验**（loop 执行前 zod safeParse，失败友好报错回填让模型自纠，MCP 宽松 schema 自动跳过）。**验证**：agent/web tsc + vite build 全过；相关套件全绿（bugfix 14 项、terminal 41、plugin 78、memory 84、subagent 94、jobs、v212、retry/fallback/compress、settings-api、mcp、web-tools、approval-policy、tools、loop-opt、tools-opt）
- ✅ **v3.0 批 11：单机增强五件套 + computer-use UI（2026-08-16，用户拍板「这些现在做吧」）**：① **computer-use UI 落地**——右侧栏「待开发」按钮激活（RightTabKind 加 computeruse；初始面板 + 新建 tab 菜单可开；新 ComputerUsePane = 截图流实时扫描（.infu-screenshots/ 2s 轮询，缩略图网格 + 大图查看）+ 当前会话 screen_* 操作日志；新 GET /api/screenshots + /api/screenshots/file）；② **记忆剪枝**（AutoMem 200 行模式：memory_read 文件 >200 行自动剪枝——保留头部 150 + 尾部 50（最近记录价值最高），中间折叠 + 提示 memory_write replace 精简）；③ **定时任务**（新 schedule.ts：5 字段 cron 解析（* / 逗号，分钟级）+ ~/.infu/schedules.json 注册表 + startScheduler 每分钟 tick（startServer 挂载）；CLI `infu schedule add/list/remove/pause/resume`；scheduler-runner.ts 无人值守执行——**无人值守审批语义定稿 = 等价 CLI -y**：low/medium 自动批准、requireExplicit 安全红线（联网/自注册）一律拒绝绝不自动放行；执行结果落会话库可查）；④ **语义检索**（semantic_search 第 41 个工具：本地 BM25 相关度排序（k1=1.5/b=0.75）+ 中文 bigram 分词零依赖，复用 v2.7 文件索引，只读进 Planner/Reviewer 白名单——与 search_code 正则互补）；⑤ **持久 shell**（run_command 加 persistent=true：常驻 shell 会话跨调用保留 cwd/env（cmd/bash spawn + 唯一标记协议），per-session Map + 任务结束清理；⚠ 安全边界：持久会话脱离沙箱（进程已起无法施加受限令牌）——描述明确警告，断网策略/审批照常）；⑥ **LSP 诊断**（lsp_diagnostics 第 42 个工具：TypeScript 自带 tsserver 零外部依赖——open + geterr 协议，语义级类型诊断（类型错误/未使用变量/隐式 any），只读进白名单；完整 LSP（跳转/补全）留后续扩展）。**实测**：semantic_search 中文「权限审批」命中 10 处、lsp_diagnostics 对 semantic.ts 无诊断（tsserver 正常工作）、持久 shell cwd 跨调用保留（pwd 验证）、schedule add/list/remove 全链路、记忆剪枝 250→204 行。工具数 40→42；npm test 33 套件全绿。**定时任务 Web UI（补完）**：设置弹窗「Agent 能力 → 定时任务」新 Tab（SchedulePane：cron+描述添加表单/列表/启停开关/两段式删除，附无人值守语义说明）+ /api/schedules CRUD（GET/POST/PATCH/DELETE）；API 实测全链路。**剩余触发条件项**：v3 团队版 + microVM（第二个真实用户）
- ✅ **v3.0 桌面化批 10：打包 + vision 底座（2026-08-16，用户拍板「打包 + vision 底座」）**：**打包**——electron-builder 26 NSIS 安装包（InFu Setup 0.1.0.exe 109MB，占位图标 build/icon.png 待用户换正式图标；electronVersion 固定 43.4.0；npmRebuild:false（node-pty 1.1 与 sandbox-rs 均为 N-API，ABI 稳定跨 Electron 免重编译，本机缺 Spectre 库正好绕开）；workspace 符号链接包无视 files 字段 → 顶层 files 排除 .infu-worktrees/src/tests/target（570MB Rust 产物）等；asarUnpack sandbox-rs+node-pty 原生模块；extraResources web dist → resources/web/dist 与 staticDir 路径天然匹配；npmmirror 镜像下载 NSIS 工具）。**打包产物端到端验证通过**（启动/API 200/React 渲染/webview+CDP 桥/浏览器任务）。**vision 底座**——① `ToolContext.visionQueue`（图片 base64 队列，loop 每轮请求前合并为 image part 注入下一条 user 消息，非视觉模型由既有降级机制兜底）；② **read_image**（第 37 个工具：读图 png/jpg/webp → 注入视觉上下文；路径越界/类型/8MB 上限校验）；③ **computer-use 三工具**（screen_capture 截屏 / screen_click 坐标点击（medium 审批）/ screen_type 键盘输入——桌面模式专属，主进程 PowerShell 零依赖实现：截图 System.Drawing CopyFromScreen、点击 user32 SendInput P/Invoke、输入剪贴板粘贴 Unicode 安全；截图存项目 .infu-screenshots/）；④ 工具数 36→40。**实测**：read_image 注入 ✓ + screen_capture 桌面截图落盘 ✓ + visionQueue 事件落库 ✓（agnes 无视觉 → 降级提示路径也通）。安全边界：screen_click/type 走 medium 审批 + 审计
- ✅ **v3.0 桌面化批 9.7：打开右侧栏不再自动开浏览器（2026-08-16，用户反馈）**：根因 = BrowserPanel「面板打开且无 tab → 主进程请求建浏览器」effect（批 8 为 Agent 链路加的）→ 用户手动打开右侧栏也自动建浏览器 tab，不显示初始选择界面。修复：删该 effect（browserOpen 无前端调用点），浏览器只由 Agent 请求（onOpenRequest）或用户显式 ➕ 创建；打开右侧栏显示初始选择界面（审查/浏览器/子 Agent）。验证：桌面重启 API 200
- ✅ **v3.0 桌面化批 9.6：➕ 修复与紧贴右缘（2026-08-16，用户反馈两连）（2026-08-16，用户反馈两连）**：① **➕ 菜单「点了没反应」**——根因：批 9.5 把 ➕ 移入 `overflow-x-auto` tab 滚动区后，CSS 强制 overflow-y:auto → `absolute top-full` 菜单被滚动容器裁剪（根本没渲染）→ 菜单移出容器改 **fixed 视口定位**（getBoundingClientRect 记录按钮坐标）；② **➕ 离右缘太远**——根因：pr-[140px] 大让位 → 移除让位，➕ 紧贴右侧边框（bottom 对齐独立于滚动区）；原生按钮悬浮区 **titleBarOverlay height 32 压缩**（与 tab 条底部内容垂直错开、背景与 bg-ink 同色视觉融合，无需大让位）。验证：编译全过、桌面重启 API 200、浏览器任务无回归
- ✅ **v3.0 桌面化批 9.5：右上角按钮布局拍板（2026-08-16，用户拍板「原生三按钮 + 折叠按钮左上方 + 新建 tab 动态跟随」）**：批 9 自绘 WindowControls 与 Windows 原生三按钮（`titleBarStyle:hidden` 在 Windows 保留原生按钮悬浮右上角）重合 → 用户拍板：**恢复原生 titleBarOverlay 三按钮**（theme:set IPC + loadConfig theme 联动恢复；删 WindowControls.tsx）；**RightRail tab 条重排**——折叠按钮移到**最左（左上角）**、新建 tab（➕）改为**动态跟随**（紧跟最新 tab 后面，不再固定右上角操作层）、右上角 pr-[140px] 完整让位原生按钮；**折叠态 rail 展开按钮下移到底部**（顶部 38px 是原生按钮悬浮区，放顶部会被盖）。三栏顶到最顶 + 拖拽区不变。验证：编译全过、桌面重启 API 200、浏览器任务无回归
- ✅ **v3.0 桌面化批 9：去独立标题栏 → ZCode 式三栏顶到最顶（2026-08-16，用户拍板）**：删 TitleBar 通栏（38px）——**三栏顶部各自成为窗口拖拽区**（Sidebar Logo 行 60px / Chat header 40px（有消息时）/ RightRail tab 条 60px，均 `-webkit-app-region: drag`，可交互元素 no-drag）；**窗口按钮改右上角自绘悬浮层**（新 `WindowControls.tsx`：min/max/close 三按钮 absolute right-0 top-0 z-50 no-drag，仅桌面渲染，主题色 CSS token 自动跟随）；main.ts 移除 titleBarOverlay 系统按钮与 theme:set IPC（loadConfig/applyThemeOverlay 清理）；RightRail tab 条右侧 pr-[140px] 预留窗口按钮空间。桌面实例验证：root html 含 WindowControls 悬浮层、浏览器任务无回归（browser.test 10 通过 / plugin.test 78 通过）
- ✅ **v3.0 桌面化批 8：架构定稿 = webview 元素 UI + 主进程 CDP 桥（2026-08-16，用户拍板「不是浏览器覆盖 infu，而是 infu 覆盖浏览器」+ 参考 ZCode 宿主注入）**：批 7 回退 WebContentsView 被用户否决（原生层形态 = 黑框/盖 DOM/菜单让位，非拍板形态）；「Bing搜索InFu失败」会话（916 事件）确认批 7 三大遗留根因（浏览器依旧被 UI 关闭：loadSession 清 rightTabs → BrowserPanel 卸载 → browserCloseAll；eval/fill 工具级 bug；webview 元素形态用户明确要）。**新架构**：① **UI = `<webview>` 元素**（DOM 层叠：圆角/阴影/菜单自然盖浏览器之上；每 tab 一个元素；自由尺寸 = 元素 CSS 无 bounds）——命门验证：webview guest 渲染进程本机加固环境崩溃，**必须 `webpreferences="sandbox=no"`**（默认 sandbox 渲染起不来 = 批 4-6 render-process-gone 根源）；② **Agent 控制 = 主进程 CDP 桥**（弃用 playwright connectOverCDP 与 remote-debugging-port：playwright 初始 target 列表过滤 webview 类型 = tab 不可见/空白堆积/输入污染/失败循环灾难根源）——did-attach-webview 注册 Map<wcId, WebContents> + `webContents.debugger.attach("1.3")` 独占，注入 `__infuCdpSend/__infuCdpOn/__infuBrowserTabs/__infuOpenEmbeddedBrowser/__infuSelectBrowserTab`（Agent 与主进程同进程直接调桥 = ZCode「宿主持有浏览器暴露给 Agent」）；③ **修浏览器关闭**：BrowserPanel 改 RightRail 常驻（webview 元素从 DOM 移除即销毁 guest → 面板只能显隐不能卸载），loadSession 清 rightTabs 只切显隐，仅用户显式关浏览器 tab（×→browserCloseAll）才销毁；④ **修 browser_eval**（Runtime.evaluate replMode：语句/表达式/函数三态通吃——旧实现只收函数表达式：`const x=…` SyntaxError、表达式 `fn is not a function` = Bing 6 连败根因）；⑤ **修 browser_fill**（页面内多级匹配 CSS→placeholder/aria-label/name/title→label→可见兜底——「输入搜索词」可命中）；⑥ **修编号点击**（click 用与展示同一份 snapshot 的 indexMap——动态页面两次快照编号漂移 → describeNode nodeId=0）；⑦ **顺手修**：server 静态托管 `isPathInside(root, abs)` 参数反序 → 生产模式 404（批 7 生产模式从未真正工作的隐藏 bug）、React key 用稳定自增 id（wcId 作 key → 元素重建 → 新 guest 无限循环 = attach 2→25+ 疑云）。**实测**：真实 Agent 任务 bing 搜索（fill 命中 placeholder + eval 语句提交）/多 tab 新建切换/browser_tabs 复用/example.com 读取全通；**跨会话浏览器 tab 保留、无空白堆积**；npm test 33 套件全绿。安全边界：CDP 桥只对 Agent 自己的 tab；browser_close 语义 = 绝不主动关闭（ZCode 语义）
- ✅ **v3.0 桌面化批 7（2026-08-16，批 8 已整体替换）**：回退 WebContentsView 被用户否决（形态非拍板）——其「输入全 JS 注入（焦点解耦）」与「重复提醒仅连续失败时注入」经验批 8 延续。**历史教训（勿回退）**：playwright connectOverCDP 对 webview 不可见 = 批 4-6 灾难根源；WebContentsView 原生层盖 DOM = 用户否决形态
- ✅ **v3.0 桌面化批 6.5：输入污染修复 + 发送键常驻 + 重复提醒优化（2026-08-16，用户四点反馈）**：① **Agent 在主窗口输入框打字（「INFU桌面版」）**——根因：onListening 第二次 loadURL（端口冲突 4317→4318）**未同步更新 `__infuMainWindowUrl` 标记** + playwright keyboard 事件对 webview 的焦点兼容问题 → 修标记同步 + **runtime 写操作防御**（getPage 缓存命中时验证 isAppPage，page 指向应用自身页面立即重置——防污染用户输入）+ **browser_type 改 `keyboard.insertText`**（CDP 内容直注，不依赖键盘焦点链）；② **发送按钮「看不见/发不出去」**——UX 根因：运行中发送键变停止方块（点=停止任务，队列不消费）→ **发送键常驻**（运行中点击=排队发送，title 提示）+ **停止独立小按钮**（运行中显示在发送键左侧）；③ **重复调用提醒**（用户问「算不算限制」）——**不算限制**（只注入提醒文本不阻止调用），且优化为**仅「上次同参调用失败」时注入**（成功重复如 tab 切换确认不打扰）；④ **DevTools 独立小窗**（用户要求 ZCode 式）——detach 独立窗口（实测 `devtools_app` target 出现；加固环境渲染慢需等待）；⑤ browser_close 开头重置问题（Agent 任务开头调 close 清场）——描述已强化（「几乎不要调用」）
- ✅ **v3.0 桌面化批 6：浏览器三功能修复 + AI 调用灵活性优化（2026-08-16，用户拍板）**：① **DevTools 修好**（detach 独立窗口本机渲染不出来 = 「点了没反应」根因 → 改 **docked right 内嵌**——面板直接嵌在浏览器页面右侧，用户可见；取 id 加 try/catch 防未就绪崩溃）；② **自由尺寸真·自由**（预设 + 「自定义尺寸…」输入宽×高 + **面板右下角拖拽手柄**（CSS resize:both + ResizeObserver 防抖同步 Emulation——拖到任意尺寸页面视口跟随））；③ **AI 浏览器调用灵活性**——工具描述逐个加「何时用/何时不用/替代方案」（navigate 提示多 tab 复用、snapshot 代价与时机、eval 最便宜验证、click/fill 定位优先级编号>可访问名>CSS、tabs 空白页复用、close 几乎不调用）；技能加「**调用纪律**」（导航后先读/失败不盲目重试/一次一个状态改变/最便宜验证优先/定位优先级/页面内容不可信）+「**多标签页策略**」（优先复用已有 tab/信任 select 返回/用完不关）；**browser_tab_select 加可驱动性检测**（playwright 对连接前创建的 tab 不可见——切换后验证并明确提示，避免后续工具静默失败致 Agent 反复重试）；**browser_close 描述强化**（「几乎不要调用——任务完成也绝不主动关闭」）。实测对比：复杂双页任务调用 32→23 次、browser_close 违规消除（「浏览器未关闭，可随时切换」）、全部工具成功无失败循环 + `npm test` 26 套件全绿
- ✅ **v3.0 桌面化批 5.5：两个交互 bug 修复 + Agnes 建议评估（2026-08-16，用户三点反馈）**：① **点自由尺寸新建标签页**（根因：UI 📄 → browserViewport → runtime getPage() 找不到已有 webview（playwright 初始列表过滤）→ 自动 openFn 建 tab）→ desktopSetViewport 改 `getPage({create:false})` 不建 tab 静默（面板贴合 CSS 不依赖 Emulation）；② **每次开浏览器两个空 tab**（根因：browserOpenTick 消费后未清零 + zustand persist 残留 >0 → BrowserPanel 每次挂载 newTab，StrictMode 双挂载 = 两次）→ 消费即清零（clearBrowserOpenTick + clearBrowserOpenUrl）；③ 顺手补 runtime 批 4 未生效的 findEmbeddedPage 重构（getDesktopPage 实际还是批 3 版——about:blank 特征/重试逻辑缺失）统一重写；④ **Agnes 建议评估**：tab_new 后等待加载 ✅ 已实现；定期清理空白 tab ❌ 不采纳（空白页是用户可见资源，自动清理有误删风险——browser_tabs 标注空白页由 Agent 判断）；browser_eval 读标题验证 ✅ 技能补示例。实测：点占位 1 tab ✓ / 点自由尺寸不新建 ✓ + `npm test` 26 套件全绿
- ✅ **v3.0 桌面化批 5：四项差距全补齐（2026-08-16，用户拍板「去做吧」）**：① **tab 协议工具**（对齐 ZCode tabs.list/get/new）——`browser_tabs`（列出全部标签页：编号/标题/URL/活跃，数据源 = 渲染进程 tabsSync 上报主进程注册表）、`browser_tab_new {url?}`（新开标签页直接加载 URL——webview src 初始化无竞态；就绪等待 = 标记变化轮询 20s + playwright 动态发现 1.5s）、`browser_tab_select`（按编号切换——主进程转发渲染进程 setActiveId，切换后 activeId effect 重新上报标记 + clearPageCache 重匹配）；② **domSnapshot 完整度**——ax.ts 重构：交互节点 [n] 编号（INTERACTIVE_ROLES 12 类）+ iframe 遍历（Page.getFrameTree → 每 frame 单独 getFullAXTree + [iframe] 标记）+ shadow DOM（AX 树天然含）；③ **viewport Agent 侧**——`browser_viewport {width,height,fit}`（桌面模式 desktopSetViewport Emulation + __infuNotifyViewport 通知 UI 面板贴合）；④ **编号错位根治**——snapshot 编号单一来源（AX 树带 [n]，删除独立 DOM 编号区），clickByIndex 改 CDP 定位（backendDOMNodeId → describeNode → resolveNode → callFunctionOn click）。**实测**：真实 Agent 多标签全流程（navigate example.com → tab_new 加载 bing → tabs 列出 5 tab → select 切回读标题 ✓；AX 树编号 `[5] <searchbox>` 显示）+ `npm test` 26 套件全绿（工具断言 8→12）。**遗留**：webview 首次初始化慢（加固环境 ~10s）、Agent 操作产生空白中间 tab（语义正常）
- ✅ **v3.0 桌面化批 4.5：浏览器工具灵活化 + 三功能验证（2026-08-16，用户四点反馈）**：① **browser_close 死板问题**（用户「打开抖音直接 browser_close 关浏览器」）——技能与工具描述改为「**任务完成不主动关闭**（用户可能继续查看），仅用户明确要求关闭/需彻底重置时调用；自主决策不是固定流程」；② **三功能实测**：自由尺寸 ✓（面板 CSS 贴合 375×656 → 适应窗口恢复）、在默认浏览器中打开 ✓（openExternal 链路）、调试工具 ✓（webview 元素 openDevTools 本机不可用 → **主进程 fromId 兜底** openDevTools({mode:'detach'})——实测 devtools target 出现）；③ **定位浮标阈值 480→560**（聊天列不够宽裕即隐藏，宽度充裕才显示）
- ✅ **v3.0 桌面化批 4：嵌入式浏览器迁移 <webview> 元素架构（2026-08-16，用户拍板「不是浏览器覆盖 infu，而是 infu 覆盖浏览器」）**：**架构反转**——浏览器从原生层（WebContentsView 永远盖 DOM）变为 **DOM 元素（webview 标签）**，InFu UI（菜单/占位/起始提示）天然覆盖其上，覆盖/刷新/布局问题全部根治（webview PoC 二次验证：**无 partition + webpreferences="sandbox=no" 可行**——有 partition 时 playwright 不可见；加固环境渲染慢但最终正常）。**主进程精简**（删 WebContentsView tabs Map/rect 上报/状态广播，只留 CDP 端口 + webContentsId 标记同步（渲染进程 webview.getWebContentsId → IPC → webContents.fromId）+ Agent 打开请求转发）；**BrowserPanel 重写**（React 管理 webview 数组 = 多 tab；导航/后退/刷新/DevTools 走 webview 元素方法；起始页 about:blank + DOM 提示层覆盖（ZCode 同款）；自由尺寸 = CSS 面板贴合 + runtime Emulation；菜单 DOM 层叠无需让位）。**踩坑三连**：① React 19 把 webview 当已知 HTML 元素用 property 设属性——webpreferences 需 attachWebview 里 setAttribute 兜底；② **webview 方法仅 dom-ready 后可用**——所有操作走「就绪后执行」守卫（whenReady 队列），且 **React state updater 惰性求值会在渲染期执行 updater——DOM 读取必须移出 updater**（否则 dom-ready 前调方法 → 组件崩溃白屏）；③ 无 src 的 webview 不初始化（不触发 dom-ready）→ 初始 src="about:blank"。**Agent 驱动适配**（playwright connectOverCDP 初始 target 列表过滤 webview 类型——只动态发现连接后新建的 → runtime「找不到 → openFn 打开新 tab → 全套重试」恰好符合 ZCode「Agent 只动自己的 tab」；openFn 转发改 **App 顶层全局监听 + store tick 信号**（BrowserPanel 未挂载时请求不丢）；closeBrowser 桌面模式重置 about:blank）。**实测**：真实 Agent 全链路（browser_navigate 打开 example.com 读标题 → browser_close）+ 多 tab/菜单覆盖（DOM 天然）/自由尺寸/起始提示 + `npm test` 26 套件全绿。**遗留**：webview 首次创建初始化慢（~10s 加固环境）、编号区与 AX 树错位
- ✅ **v3.0 桌面化批 3.5：黑界面治理 + 自由尺寸面板贴合（2026-08-16，用户「黑色界面非常烦」反馈）**：① **刷新后黑界面消失**（根因：WebContentsView 在主进程，渲染进程刷新不影响它——页面 reload 后视图仍盖在右侧栏）→ App.tsx 挂载时检测 `performance navigation.type === 'reload'` → browserCloseAll；② **点击才开**（BrowserPanel mount 时 tabs 为空不自动 open——占位「点击打开嵌入式浏览器」，点击/Agent 驱动才开；tabs 已有（切 tab 回来）自动恢复显示）③ **📄 自由尺寸 = 面板贴合视口**（对齐 ZCode free-size：选 375×667 时视图 bounds 收缩为 375×667 居中 + Emulation 内容模拟；适应窗口 = 清 freeSize + 清 Emulation；主进程 lastRect + freeSize 状态，rect 上报时 applyActiveBounds）④ 修 tab 标题显示 data: URL（起始页无 title 时 getTitle 返回 URL——tabTitle 优先判 url 前缀）。实测：刷新后视图清空 + 占位 + 点击打开 + 375×667 贴合 + 适应窗口恢复全链路 ✓
- ✅ **v3.0 桌面化批 3：嵌入式浏览器对齐 ZCode 布局（2026-08-16，用户实测反馈）**：**多 tab 架构**（主进程 WebContentsView tabs Map：新建/切换/关闭/全部关闭 + 状态广播 {tabs[], active}；`__infuBrowserWebContents` 始终指向活跃 tab——runtime 精确匹配无需改）；**BrowserPanel 重构对齐 ZCode 布局**（内部 tab 条：标题+×+➕ 新建；工具栏：◀▶↻ 地址栏 📄 尺寸 ⋯ 更多；**起始页美化**：🌐 图标 +「浏览器 / 粘贴或输入 URL 以打开网页」，地址栏过滤 data:/about:blank 显示占位；**📄 自由尺寸**：视口预设 375×667/375×812/768×1024/适应窗口——CDP Emulation 必须走 playwright CDPSession（**Electron webContents.debugger.attach 在 connectOverCDP 会话存在时静默失败**）→ runtime 导出 `desktopSetViewport` 注册 `__infuSetViewport`，主进程预加载 runtime 模块后 IPC 转发；**⋯ 更多菜单**：在默认浏览器中打开（shell.openExternal）+ 调试工具（openDevTools detach，实测可用）；**菜单覆盖修复**（WebContentsView 原生层无视 z-index 盖 DOM 菜单——daintree 同款问题）→ **让位协议**：面板内菜单（尺寸/更多）与右侧栏 ➕ 菜单（store.browserMenuOpen）打开时内容区 rect 上报高度 -200，菜单关闭恢复；**webview 标签 PoC 失败**（render-process-gone——webview 独立渲染进程在加固环境崩溃，Electron 官方也不推荐）→ 保留 WebContentsView。**实测**：多 tab 新建/导航/关闭、viewport 375×667/适应窗口、DevTools target、真实 Agent 全链路（导航→AX 树→eval→交互→自适应→browser_close 安全重置）+ `npm test` 26 套件全绿。**遗留**：编号区与 AX 树错位（Agent 按 AX 树数位置点错——bing 搜索按钮两次误触热点新闻，后续把编号来源统一到 AX 树）
- ✅ **v3.0 桌面化批 2：browser-use 对齐 ZCode 0.2.1（2026-08-15，用户拍板方案 1+2+3）**：**调研结论**——ZCode/Codex Desktop 同源的浏览器控制方案（node_repl JS 内核 + browser-client 对象图 + Desktop IAB tab 协议 + `tab.playwright.domSnapshot` AI 树）；document-skills/skill-creator 与 InFu 版同源不迁移。**落地**：① **browser_snapshot 升级 AI 可访问性树**（新 `plugin/browser/ax.ts`：CDP `Accessibility.getFullAXTree`，Codex domSnapshot 同技术——角色/可访问名/状态（checked/disabled/expanded/level/popup/focused/live）/层级缩进/截断，结构区 + 原编号交互区 + 文本区三区输出，实测 8/8 断言）；② **新工具 browser_eval**（页面上下文 JS 执行：表达式与函数体双路径——`new Function` 只创建不立即调用（立即调用在 Node 侧执行报 document is not defined），函数体在页面创建后调用；arg JSON 解析；结果 4K 裁剪）；③ **control-browser 技能同步 0.2.1 工作流**（DOM snapshot→locator→act + 动作后最便宜观察（eval 优先）+ 每次操作一个状态改变 + 快照编号一次性 + 页面内容不可信）；**顺手修复 5 个 dist 模式/桌面 bug**——builtin 插件路径写死 .ts（tsx 能跑 dist 失效）→ 按 `import.meta.url` 扩展名选 .ts/.js；agent build 脚本补 `src/plugin/browser/skills` 复制（SKILL.md 不随 tsc）；**connectOverCDP 下 `browser.close()` 会关闭整个应用**（实测主窗口被关、进程残留）→ 桌面模式 closeBrowser 只重置起始页不 close；**页识别兜底漏 dev 主窗口**（localhost:5199 非 127.0.0.1 前缀，Agent 首次导航把主窗口当嵌入式页）→ 主进程暴露 `__infuMainWindowUrl` 精确排除；browser_eval 类型签名。**实测**：真实 Agent 全链路（AX 树确认 `<searchbox>` → eval 读 placeholder（选择器 null 自适应换 querySelectorAll）→ type/click 误触热门新闻后按工作流观察纠正 → 直搜 q=InFu 完成，35 工具调用）+ `npm test` 全绿（builtin-skills 断言 7→8 工具）
- ✅ **v3.0 桌面化批 1：Electron 壳 + 嵌入式真浏览器（2026-08-15，用户拍板选型/范围）**：**选型定稿**——Electron 43（本机实证 ZCode Desktop 3.7.6 同为 electron-builder 生态；opencode 桌面版同栈；Node 后端零改造宿主 + 嵌入式浏览器 = 连接应用自身 Chromium）**新 `packages/desktop/`**（主进程 ESM + preload.cjs）：`startServer({staticDir, onListening})` 同进程宿主（server.ts 加静态托管 + CORS + 端口回传，前端相对路径零改动）；**无边框自定义标题栏**（titleBarStyle hidden + titleBarOverlay，拖拽/贴边/双击系统保留，overlay 按钮色随主题联动 IPC）；**嵌入式真浏览器**（WebContentsView：rect ResizeObserver 实时上报 + 地址栏/前进后退/刷新/DevTools + window.open 视图内导航 + 导航白名单；browser-use runtime 桌面模式 = `connectOverCDP` 连应用自身 CDP（9222 占用自动递增），页识别 = globalThis 标记 webContents.getURL 精确匹配 → data: 起始页 → 非主窗口页兜底，**Agent 页面即用户页面实时跟随**）；**托盘仅退出入口 + 单实例 + 窗口状态持久化**；**本机加固适配**（sandbox:true 渲染进程无法启动「操作不被支持」→ sandbox:false + contextIsolation 保留；GPU 子进程反复 0x80000003 断点崩溃 → disableHardwareAcceleration + in-process-gpu + crash-limit 兜底 + ready-to-show 2s 兜底——**in-process-gpu 必须配 sandbox:false**，sandbox:true 时渲染 CDP 挂起）；**dev 架构**（vite 5199 错开常驻 5174；apiFetch 读 `?infuAgentPort=` query 绝对地址直连 + CORS，生产同源零改动）；shared 补 AppearanceConfig.theme 接口缺失（v2.8 只加了 schema）。实测：CDP 全链路（窗口/桥/API/标题栏）+ playwright 驱动嵌入式浏览器（导航/搜索交互/截图）+ **真实 Agent e2e**（聊天「用浏览器打开必应搜索 InFu」→ browser_navigate 实时驱动窗口内页面）+ `npm test` 全绿（修复 session-store 断言未同步 v2.14 批 10 rewind marker 事件）。**遗留（批 2）**：打包（electron-builder NSIS + 图标）、computer-use / read_image（vision 底座）、桌面端测试套件
- M1：AI 接入层（任意大模型）、10 基础工具、Agent 循环（30 步上限+进度总结）、CLI、Hono/SSE 服务
- M2：Web 三栏 UI（对话/工具过程/Diff）、停止按钮（abort 全链路）、审批队列
- M3：**沙箱 L1**（环境变量消毒/敏感路径写保护/命令审计）+ **沙箱 L2**（Docker：断网/只读挂载/资源限制/任务后销毁）、**交付报告**（buildReport，任务结束自动生成）、**模型管理 Web UI**（CRUD API + 弹窗）
- git worktree 任务工作树：每任务独立分支 + 工作树，任务后手动合并/丢弃（server 三端点 + Web 开关/操作条）
- M4：**模板任务引导**（4 模板：初始化项目/修复测试失败/分析项目/添加功能；Web 空态欢迎面板 + CLI `--template`）+ **Planner/Reviewer 分层编排**（`orchestrator.ts`：Planner 只读规划 → 计划确认 → Executor 全工具执行 → Reviewer 只读审查 → 汇总报告；Web 输入框旁**三档模式选择器**（编排/直接/方案，Shift+Tab 切换）+ **计划卡片**（可编辑、批准后执行，`POST /api/plan/:id`）；CLI `--no-orchestrate`/`--no-plan-approval`）
- M6：**网络出站软控制策略**（本机实测全部 OS 级按进程断网路线不可行——WFP 引擎拒绝/LSA 特权被加固删除且授不回去/AppContainer 低盒 WithTokenW 1314/SYSTEM 辅助触发通道全被硬化封死/未装 Docker，详见 ROADMAP；落地为命令级策略 `net-policy.ts`：外传命令 curl/wget/nc/ssh/powershell 网络调用等默认拦截，`network=true` 经人工审批放行（🌐，-y 不放行），审计 `egress-blocked`；OS 级断网的正确姿势 = Docker L2 `--network none` 或未来云版 microVM）
- M5：**沙箱 L1.5 Windows 硬沙箱**（Rust 原生 `packages/sandbox-rs/`：CreateRestrictedToken 四标志 + Job Object 资源上限/杀整树 + 降级阶梯 full→reduced→basic→job-only；win32 自动启用；`INFU_SANDBOX_RESTRICTED=0` 禁用；run_command 与 run_test 统一走 `execLocal` 分派，修复了 run_test 绕过沙箱的缺口）+ **`/best-of-n` 并行尝试**（CLI `--best-of-n <N>`：N 路独立 worktree 并行完整编排，评分择优：测试×40/工具成功率×25/报告×20/效率×15，任务后保留 worktree 手动合并/丢弃）
- 后台日志：`~/.infu/logs/agent.log`（全事件）+ `commands.log`（命令审计，含沙箱档位）
- ✅ **v1 收官（2026-08-12）**：M1–M6 全部落地（模型接入/10 工具/Agent 循环/Web UI/审批/沙箱 L1·L1.5·L2/编排/模板/best-of-n/网络软控制），`npm test` 68 项全绿，CLI+Web 端到端实测正常。剩余项（云版/microVM/WSL2 沙箱/best-of-n Web UI）均为 v1 范围外，见 ROADMAP
- ✅ **v2.1 持久化与会话（2026-08-12）**：SQLite 会话库 `~/.infu/infu.db`（node:sqlite，零依赖，**需 Node ≥22.5**，本机 24）+ 全量事件流落库（tool-result 存完整输出，Diff 面板升级为完整 diff）+ 会话 API（列表/详情/删除/Rewind）+ Web 左侧栏会话列表（新建/切换/删除/状态徽标）+ 继续会话（历史回顾注入，消息级上下文重建留 v2.2）+ 消息轮次内嵌「回滚到此」按钮（两段式确认；检查点 = user-message/step-start 事件）+ CLI `infu sessions` / `--session <id>` + v1 localStorage 数据一次性迁移 + 配置 zod schema 基础（version 字段/损坏自动备份/未知字段保留）。`npm test` 86 项全绿 + 真实模型端到端实测（建会话/继续/回滚/Web 重放）
- ✅ **v2.2 批 1 可靠性核心（2026-08-13）**：API 失败自动重试（`chat.ts` 重构：429/5xx/408/网络/超时指数退避 1s/2s/4s+jitter，尊重 Retry-After；已产出 delta 后断流不重试）+ **降级备用模型链**（新 `providers/gateway.ts`：`ModelChain` 重试耗尽依次切换、任务内保持不自动回主模型；配置 `ModelConfig.fallbackModelIds` + CLI `--fallback-model`（可重复）+ Web 模型弹窗「备用模型」多选 + `model-fallback` 事件落库/Timeline 徽标/CLI 打印）+ **消息级上下文重建**（新 `db/rebuild.ts`：事件流 → OpenAI wire messages，callId 消费式配对/缺失补占位/孤儿丢弃/reasoning_content 保留）+ **断点恢复**（继续会话升级为消息级重建续跑，不重放工具副作用；CLI `--session` / Web 带 sessionId 自动获得）+ 顺手修复（loop baseURL 硬编码 deepseek 兜底 bug → `resolveBaseURL` 统一解析；CLI 参数值混入 prompt 的既有 bug）。`npm test` 138 项全绿（新增 retry/rebuild/fallback 52 项）+ CLI 端到端实测（真实模型建会话/继续重建/fallback 解析/未知 id 警告）
- ✅ **v2.2 批 2（2026-08-13）**：**上下文压缩按模型因地制宜**（新 `agent/context.ts`：`resolveContextWindow`（显式 > 模型名表 > provider > 128k）+ 超「当前活动模型窗口×80%」触发压到×60%，降级切模型预算跟随，摘要失败降级丢弃最老，DB 事件流无损）+ **动态步数**（新 `agent/steps.ts`：显式 `--max-steps` > Planner 建议【建议步数】N > 启发式 > 30）+ **轻量模型选择按角色路由**（`InfuConfig.roles`/`ModelConfig.roles`/CLI `--planner-model` 等/API `roleModelIds`；`phase-start` 带 `model` 字段；角色独立降级链）+ **provider 兼容矩阵**（`npm run probe -- <modelId>` 探针，deepseek 5/5；`docs/PROVIDER-MATRIX.md` 模板，其余 provider 待 key 实测回填）。`npm test` 179 项全绿（新增 compress 24/steps 17）+ 端到端（动态步数 Planner 建议生效/角色路由打到 glm 端点）。**遗留：Web 角色 UI 形态实施前单独讨论**

- ✅ **方案模式升级 + 计划确认三态化（v2.3，2026-08-13）**：① 方案模式（suggestOnly）从「无工具纯文本」升级为「只读工具 + run_test」（写工具/命令工具架构级不注入，绝不改文件）；② **计划确认三态**（新 `agent/plan-feedback.ts`）：用户自由文本回复 → 模型判断 `execute/revise/abort`——execute（回复作指示注入执行）/ revise（Planner 按意见重新规划再确认，最多 2 轮）/ abort（任务停止，不执行不审查；"先停下来先不做"实测正确中止）；Web 计划卡片改「提交/取消」（回复输入框 + 计划可编辑），CLI 交互输入回复文本（空=批准）；修复 CLI 双 readline 抢 stdin bug（makeDecider 统一 getLines 单实例）；③ **回滚按钮移到用户消息**（回滚语义 = 撤销这条指令及其后内容，微信撤回式；不再挂在 assistant 消息）。缓存/产物策略借鉴生态共识：不自动清理项目内产物，隔离靠 worktree
- ✅ **角色路由面板（v2.3，2026-08-13）**：Web 模型管理 → 模型 Tab「角色路由」面板——规划/执行/审查 三行各选模型 + 独立思考级别（1-4，未设置跟随默认/全局）；config `roles` 支持 `{model, thinkingLevel}` 对象形态（zod union 兼容旧 string）；`PUT /api/roles` / `GET /api/roles` 端点；orchestrator 角色级 thinkingLevel 优先于全局；resolveRoleThinking 解析。验证：角色路由端到端（规划阶段真实切到 local-qwen）+ 232 项测试全绿
- ✅ **模型管理重构（v2 供应商凭据，2026-08-13）**：config 升级 v2（`providers[]` 存凭据 + `models[]` 经 `providerId` 引用，**模型层不再存 API Key**；v1 配置读取时在线迁移归并，写回持久化）+ **供应商模板表**（`providers/templates.ts` 8 家：选类型自动填 baseURL/默认窗口/思考级别，2026-08 联网调研校准：deepseek/GLM-5.2/GPT-5.6/Claude5/Gemini/Kimi 均 1M 窗口）+ **上游模型获取**（`POST /api/providers/:id/models` 拉 OpenAI 兼容 `/models`，Web 勾选启用，模板预设匹配窗口/级别）+ **思考级别 4 档 UI**（`mapThinkingLevel`：UI 1→最弱、2-4 按比例映射到模型实际级别数；`buildThinkingParams` 按供应商协议注入：deepseek `thinking.type`、openai/kimi `reasoning_effort`、zhipu `thinking+effort`、qwen `enable_thinking`、google `thinkingConfig.thinkingLevel`；CLI `--thinking 1-4` + Web 输入框旁选择器 + 4 档按钮）+ **Web 模型管理弹窗双 Tab**（供应商：模板添加/获取模型勾选/删除连带模型；模型：窗口/思考级别/备用模型编辑）+ ChatPanel 输入框左下方模型选择器（供应商分组 + 思考级别）。`npm test` 226 项全绿（新增 config-migration 15 / thinking 24 / providers-api 7）+ 浏览器实测（真实 DeepSeek 上游拉取 v4-flash/v4-pro）+ config 迁移实测
- ✅ **v2.3 批 1 MCP 客户端（2026-08-13）**：`@modelcontextprotocol/sdk` 1.30 + 新 `src/mcp/`（client：stdio 子进程/Streamable HTTP 双传输 + 20s 握手超时；schema：JSON Schema→zod 转换器（未知回退 z.any）；tools：ToolDef 适配器——**默认 medium 审批**防 prompt 注入投毒，`riskOverrides` 工具名精确 > 前缀*通配；index：`loadMcpTools` 只连 enabled/失败跳过不阻塞/重名加服务器前缀/任务结束统一 close 防残留子进程）+ **注入范围仅 Executor 阶段与直接模式**（Planner/Reviewer 架构级只读不暴露；suggestOnly 与 /best-of-n 不注入）+ **审批/审计复用现有通道**（approval-required/result 事件 + tool-start/tool-result 全量落库 = 会话回放审计）+ **CLI** `infu mcp add/list/remove/status`（交互向导 + --type/--command/--args/--url 直传）+ **API** `/api/mcp` CRUD（env 脱敏只回键名）+ `POST /api/mcp/:id/tools` 探测（15s 超时）+ **Web 顶栏「MCP」按钮独立弹窗**（列表/启停开关/探测工具+风险徽标/添加表单/两段式删除）+ config `mcpServers[]` 节（passthrough 兼容，无需迁移）+ **阶段级精确续跑**（v2.2 遗留落地：`inferResumePhase`——尾部 planner/executor 且有计划 → 跳过规划直接 Executor 续跑，计划沿用上次确认的；无计划/reviewer 尾部/直接模式从头）。`npm test` 290 项全绿（新增 mcp 58 项）+ **端到端实测**：CLI（真实 stdio server：greet 注入调用成功 / add_note medium 审批拒绝与批准 / 文件落盘 / 事件落库可回放）+ Web（弹窗列表/启停/探测工具+风险徽标 / 任务中「MCP 服务器已连接」事件 / 模型调用 greet → medium 审批弹窗 → 批准 → 回复回填 + 交付报告）。**安全边界**：MCP 服务器子进程不受 L1.5 沙箱约束（配置即信任，工具层审批兜底）；Windows 下 npx 需写 npx.cmd——详见 docs/MCP.md
- ✅ **自注册闭环增强（v2.3 批 1 追加，2026-08-13）**：新工具 `mcp_register`（第 11 个内置工具；opencode config-hook 自注册模式）——**Agent 可自主「编写 MCP server → 注册给 InFu 自己用」**：`src/mcp/register.ts` 白名单只写 `mcpServers` 节（models/providers/roles/apiKey 不可达，防自我提权/投毒）+ **high 级 requireExplicit 审批**（-y 不放行，与联网放行同级；Web 弹窗人工确认）+ 校验与 CLI/API 一致（id 生成/command 或 url 非空/重名拒绝）。**闭环实测**：真实任务中 Agent 参考项目内示例自主编写 `packages/agent/tests/fixtures/self-mcp-server.mjs`（get_time 工具）→ 调用 mcp_register 注册 `my-time`（high 审批批准）→ 下一任务自动注入并成功调用 get_time。`npm test` 306 项全绿（register 新增 16 项）
- ✅ **v2.3 批 2 插件系统 v1 + 钩子 + skill（2026-08-13）**：**插件协议**（opencode 式 JS 模块，`PluginDef`：tools 数组或延迟函数/hooks/skills；config `plugins[]` 节；`loadPlugins` 失败跳过不阻塞、重名加前缀、工具 risk 缺省 medium、只注入 Executor 与直接模式）+ 内置工具 **`plugin_add`**（第 12 个：high + requireExplicit + 白名单写 plugins 节——Agent 自主装插件闭环）+ CLI `infu plugin add/list/remove/status`（含探测）+ API `/api/plugins` CRUD+probe；**函数式钩子**（opencode 式）：`preToolUse`（block 拦截/改 args）/`postToolUse`（改 result）挂 loop 统一执行段（对全部工具含 MCP 生效），抛错放行不阻塞，`applyPreToolUseHooks/applyPostToolUseHooks` 导出可测；**skill 加载**（SKILL.md 社区标准 agentskills.io，progressive disclosure 三级：描述常驻 Executor system + 内置工具 **`use_skill`**（第 13 个，low 只读，进 Planner/Reviewer 白名单）读全文 + 按需 read_file references/scripts；`~/.infu/skills/` > `<root>/.infu/skills/` > config `skills[]`；CLI `infu skill add/list/remove` + API `/api/skills`；Web 管理 UI 留 v2.4 设置界面）。`npm test` 367 项全绿（新增 plugin 61 项）+ 端到端实测（示例插件工具调用 + preToolUse 钩子拦截、SKILL.md use_skill 读取）。安全边界：插件代码在 Agent 进程内运行（比 MCP 信任级别更高，配置即信任），详见 docs/PLUGINS.md
- ✅ **技能自编写闭环 + 写保护精确化（v2.3 批 2 追加，2026-08-13）**：skill 走**文件系统即注册**——Agent 用 write_file 写 `<root>/.infu/skills/<name>/SKILL.md` 即自动发现（下一任务描述注入 system + use_skill 可用），无需注册工具。为此修复 `isProtectedPath`：`.infu` 写保护从「任意层级匹配」精确到**仅用户级 `~/.infu`**（项目内 .infu/skills 是合法场景放开写；~/.infu 配置/凭据/日志仍保护）。实测：Agent 自主编写 review-checklist 技能 → 下一任务自动发现 → use_skill 读取 → 按清单真实执行代码审查。测试新增写保护断言 6 项（plugin 套件 67 项）
- ✅ **扩展管理 Web 弹窗（v2.3 批 2 补完，2026-08-13）**：顶栏「扩展」按钮 → `ExtensionsModal`（双 Tab：插件/技能，Dark OLED 骨架）——插件 Tab（添加 id+path / 启停开关 / 「加载」探测展示工具列表含风险徽标 + 钩子 pre×N/post×N 徽标与详情 / 两段式删除）、技能 Tab（列表含来源层级徽标（用户级/项目级/显式引用）+ 描述 + 移除显式引用、添加 name+path 可选）；浏览器实测通过（sample 插件工具/钩子展示、sample-skill 添加与列表）。至此 MCP/插件/技能三通道 Web 可视化齐全（钩子为插件属性，在插件卡片内展示）
- 📌 **钩子形态选型定稿（2026-08-13 联网调研）**：ZCode/Claude Code 的 hooks 是与插件**分离的独立系统**（config 直配 + 子进程 JSON 协议 + user/workspace/plugin 三层，详见 ROADMAP v2.3 批 2）；InFu 定稿 **opencode 式统一**（钩子 = 插件内 JS 函数，函数式/热加载/简单）。「零插件配钩子」的独立 config 通道**不做**，触发条件 = 真实多端共享钩子/团队策略需求（届时评估命令式兼容档）
- ✅ **v2.4 批 1 设置界面（2026-08-13）**：config 四节（approvalPolicy/sandbox/general/appearance，全 passthrough）+ **审批策略核心**（`approval/policy.ts`：三档 auto/smart/confirm + 工具覆盖/禁用（精确>前缀*>默认）+ 命令白名单 glob；guard 加 tool 参数接线、run_command 白名单豁免、loop 层禁用兜底、CLI/server 档位接入；顺手修 DANGEROUS 正则 \b 漏检）+ **沙箱档位**（SandboxMode 加 restricted 独立成档、resolveEffectiveMode 纯函数、显式 soft 不再隐式 L1.5、env 优先 config）+ `GET/PUT /api/config`（白名单四节 + defaultModelId，strip 防提权；saveConfig 4 拷贝收敛 registry）+ **Web 设置弹窗**（顶栏设置按钮 → w-[820px] 左导航大弹窗：常规/权限/沙箱/外观/模型 5 Tab，L2 档「当前机器不可用」徽标，外观字号/光标即时应用）。`npm test` 501 项全绿（新增 approval-policy 54/sandbox-config 29/settings-api 45）+ 浏览器实测（档位/覆盖/白名单/L1.5 沙箱保存落盘 curl 验证）
- ✅ **v2.4 批 2 Web 交互式终端（2026-08-13）**：`terminal/`（node-pty 真实 PTY：Windows ConPTY；会话 Map + 输出环形缓冲 SSE 重连重放 + 服务退出清理）+ 端点（创建/输入[命令级高危审批协议：未 confirmed 拦截 requireApproval]/resize/SSE 流/删除）+ **SSE 传输链路修复（关键）**：@hono/node-server 在 Node 24 下 chunked SSE 数据滞留（最小复现 serve()+streamSSE，与业务无关）→ 服务启动改**原生 Node HTTP 转发**（forwardResponse：Web Stream→socket + 背压/断开处理），Hono 路由与 streamSSE 不变，实测终端/chat SSE 均正常 + 前端 TerminalPanel（xterm 底部通栏 + 右下角入口；本地预览缓冲 + 转义序列透传修复 focus 报告污染；高危确认框拒绝/允许；串行写入队列）+ docs/TERMINAL.md（安全边界：终端直连不走 L1.5，高危审批 + 全量审计兜底，白名单不豁免终端）。`npm test` 542 项全绿（新增 terminal 41）+ 浏览器实测（输入回显/高危确认/审计落盘）
- ✅ **设置界面信息架构升级（v2.4 追加，2026-08-13，用户定稿三组导航）**：设置弹窗左侧导航按三组组织——**基础设置**（常规/外观/模型设置/浏览器[规划中]）、**Agent 能力**（记忆[规划中]/插件/技能/子智能体/MCP 服务器/命令/钩子）、**数据与统计**（索引库[规划中]/使用统计[规划中]）；MCP/插件/技能/钩子从独立弹窗**内嵌**进设置（新 `SettingsPanes.tsx`：McpPane/PluginsPane/SkillsPane/HooksPane/ComingSoonPane，删除 McpManagerModal/ExtensionsModal）；「命令」= 原权限 Tab 重组（审批档位 + 工具覆盖/禁用 + 命令白名单 + 沙箱等级 + 高危命令红线说明）；未实现功能显示**禁用态占位 + 规划中徽标**；顶栏「扩展」「MCP」按钮改为打开设置并定位对应 Tab；**追加（用户定稿「对号入座」）**：模型管理弹窗彻底内嵌（ModelManagerModal → ModelPane），顶栏「扩展」「MCP」「模型管理」三按钮**全部删除**，仅剩「设置」单一入口
- ✅ **v2.5 子智能体（opencode 式委派，2026-08-13）**：**delegate_task**（第 14 个内置工具，high）——独立上下文 + 结果回收 + **同轮多工具调用并行执行**（loop 3.2 段 Promise.all，对齐 ZCode「同一消息多个工具调用并发运行」，`ctx.callId` 按调用隔离）+ **tasks[] 并行批量**（不同任务同时跑，最多 6）；**agent 文件化定义**（`~/.infu/agents/<name>.md` > 项目 `.infu/agents/<name>.md`，文件系统即注册；frontmatter：description/tools/model/maxSteps/thinkingLevel/permission(allow|ask)/sandbox）+ **内置 agent 对齐 ZCode**（`general-purpose`=全工具复杂多步任务 / `explore`=只读 7 件；调用时机经 ZCode 本机 33 次调用实证归纳：explore 67% 只读探索调研、general-purpose 33% 深度审计/审查，delegate_task 描述 + buildAgentsPrompt 注入调用时机引导）；**审批对齐 ZCode**：只读委派免审批（explore/只读白名单）、写能力委派一次授权审批（描述含工具范围）、内部继承授权不逐个弹（requireExplicit 安全红线仍逐条）、permission:ask 可要求内部逐条、agent 名不存在直接报错不弹无效审批；**展示对齐 opencode/Claude Code**：主对话流只显示子智能体条目（一行：名称/状态/委派任务），点击 → 右侧栏弹窗 = 完整消息流（思考/文本/工具过程与父 Agent 一致，实时流式）+ 最终摘要；内部过程不进主对话流（事件带 subagentId 全量落库可审计可重放）；**摘要完整接收**：system 注入输出约定（结构化摘要 ≤2000 字），20K 兜底截断；**设置面板可编辑**（新建/编辑/删除 agent：工具多选/权限/沙箱/模型/推理强度/角色提示词，POST/DELETE /api/agents）；安全边界：深度 1 不可再委派、delegate_task/mcp_register/plugin_add 架构级排除、root 越界拦截。**best-of-n 按用户评审完全移除**（同任务多路竞速多余：删除 CLI `--best-of-n`/server 分支/Web 第 4 档+TrialsPanel/tests/parallel.test.ts/docs/BEST-OF-N.md；真并发 = delegate tasks 不同任务并行）。`npm test` 643 项全绿（subagent 套件 94 项：解析/内置 agent/免审批/并行/级联停止/readOnly 徽标/深度/越界/rebuild 跳过）+ 端到端（CLI 委派真实模型回收 + 浏览器：只读委派免审批/主对话流条目/设置面板表单）。docs/SUBAGENTS.md 重写
- ✅ **v2.6 批 1 记忆核心（2026-08-13，用户拍板五层设计）**：分层经调研定稿——L0 项目指令 INFU.md（`<root>/INFU.md` 优先、AGENTS.md 兜底，全量注入所有阶段 system，32KiB 上限）+ L1 全局记忆 `~/.infu/memory/` + L2 项目记忆 `.infu/memory/`（主题文件 conventions/lessons/preferences，首次访问自动建模板）+ L4 项目历史 `.infu/history/`（任务完成自动沉淀，只增不改）+ L5 会话历史 SQLite（已有）；**任务/项目历史/会话历史三个独立维度**（对齐 Claude Code Tasks API：任务≠会话，跨会话存活——任务记忆 .infu/tasks/ 批 2 实现）；**路径作用域**（INFU.md 声明式 `- 允许/禁止: glob`，deny>allow、白名单模式，工具层接线 read/write/edit/list_directory，ToolContext.scopeRules）；**记忆工具**（`memory_read` 第 15 个 low 进 Planner/Reviewer 白名单 / `memory_write` 第 16 个 medium；~/.infu 写保护精确化：isProtectedPath 不变，memory_write 是全局记忆唯一合法通道，topic 白名单防穿越）；**自动沉淀**（`src/memory/sediment.ts`：report 后归档 .infu/history/YYYY-MM-DD.md，标题/元数据/改动概览/报告全文/审查意见，零额外模型调用，失败不影响交付；orchestrator 内部 + CLI/server 直接模式三处挂点）；Executor system 注入记忆引导段（发现稳定约定主动 memory_write）。`npm test` 720 项全绿（memory 套件 77 项）+ CLI 端到端实测（agnes 三连：任务 1 memory_write 约定 → 任务 2 memory_read 读回（跨任务闭环）→ 任务 3 secret/** 禁止路径被工具层拦截且 Agent 遵守）。**遗留（v2.7）**：记忆索引/剪枝机制（Codex 30 天剪枝/AutoMem 索引 200 行模式）
- ❌ **v2.6 批 2 任务看板（2026-08-13）→ v2.6.1 按用户评审整体删除**：「任务=会话」误称产物（.infu/tasks/ + task 工具 + Kanban 视图 + rubric 全部移除），会话管理（隶属/顶置/归档/重命名）取代其功能
- ✅ **v2.7 批 1 插件/技能生态落地（2026-08-13，借鉴 zcode 官方插件 zcode-plugins-official）**：**官方能力统一为「内置插件」**（对齐 zcode：插件=分发单位，内容可以是工具/技能）——3 个官方插件随 InFu 分发、默认启用、设置界面插件列表可见可禁用：**browser-use**（playwright-core 1.62 驱动 chromium：browser_navigate/snapshot/click/type/fill/screenshot/close 7 工具，navigate 联网门禁 high+requireExplicit、交互 medium 审批、截图存 .infu-browser/；chromium 自动探测 ms-playwright；control-browser + web-gui-tester 两技能随插件）、**document-skills**（docx/pdf/pptx 从 Anthropic 官方 document-skills 复刻，35+31+5 文件 + Python 脚本，补装 python-pptx/reportlab/pypdf，SKILL.md 加 InFu 环境适配头）、**skill-creator**（引导 Agent 写高质量 SKILL.md，适配 InFu 目录）；**插件技能挂载机制**（loadPlugins→skillDirs→registerPluginSkillDirs 注册表 + listSkills level=plugin；SkillMeta level 加 builtin/plugin）；**插件市场雏形**（marketplace.ts 内置注册表 + PluginConfig source/version 元数据 + infu plugin marketplace/install 一键安装）；**内置插件合并加载**（mergeBuiltinPlugins：默认启用，config {id,enabled:false,source:builtin} 禁用；/api/plugins 合并视图 + PUT/DELETE 内置只能启停；probe 传 mergeBuiltin:false 只探测目标）；**YAML 折叠块解析增强**（parseSkillFrontmatter 支持 description: > 块标量 + 容忍 BOM 前缀）；**skill 导入/导出**（infu skill export <name> 复制目录分享 / import <目录> 装到 ~/.infu/skills/）；**设置界面「浏览器」「记忆」「索引库」「使用统计」四 Tab 填充**（原「规划中」占位全部落地：浏览器 Tab = 浏览器控制开关（启用 browser-use 插件）+ 无头/有头 + 浏览器路径 + 浏览器数据（清除缓存/清除全部数据），配置持久化 config.browser 节；记忆 Tab = 四层记忆说明 + 自动沉淀开关（config.memory.autoSediment）+ INFU.md 指令查看 + 全局/项目记忆主题查看；索引库 = 轻量文件索引（src/index/：扫描项目文件清单持久化 ~/.infu/index/，search_code 优先复用加速，状态 + 重建）；使用统计 = 会话事件流聚合（tokens 字符/4 估算、会话/消息/活跃天数/连续天数/最常用模型/按天趋势/模型用量，GET /api/stats）。新增 GET /api/browser/status + POST /api/browser/clear + GET /api/memory + GET /api/stats + GET/POST /api/index 端点）。`npm test` 全绿（新增 builtin-skills 11 / browser 10 / settings-api +17）
- ✅ **主流式流程改造（2026-08-13，用户评审「三档太死、恢复主流 Agent 做法」）**：默认流程改为**单一 Agent 循环直接执行**（主流 Agent 做法：Claude Code/Codex/Gemini 默认直接干活）——模型自主决定寒暄直接回复/复杂任务自主探索建清单执行；**不再强制 Planner→计划确认→Executor→Reviewer**（原"发嗨也跑流程"根因：Web 硬编码 planApproval:true + 每任务强制流水线）；**Planner/Reviewer 保留为显式可选**（CLI --orchestrate / API orchestrate:true 显式开启）；清理三档残留（web store orchestrate/mode、server mode 硬编码、api planApproval:true）；DEFAULT_SYSTEM_PROMPT 加消息类型判断（非开发任务直接回复不调工具）+ todo_write 引导；Executor 提示词兼容无计划场景（自主规划）

- ✅ **v2.14 批 18 审批档位图标 + 设置有效性审计（2026-08-15）**：档位图标——全自动 = ShieldAlert（盾牌+感叹号）警告色、智能 = Scale 中性、全部确认 = ShieldCheck 中性（只有警告用色）；**设置审计**（explore 深扫 14 类设置项全链路：12 项真实生效 + 修复 3 个问题）——**defaultModelId 死配置**（Web 会话从不读取 → App.tsx fetchConfig 应用）、**子智能体 sandbox 档位死配置**（agent 文件解析无消费 → 全链路接线：ToolContext.sandboxMode → execLocal modeOverride → run_command/run_test → subagent 传 agentDef.sandbox）、**索引库面板作用域错位**（/api/index 固定启动目录 → 端点接受 root 参数）。至此**设置项全部真实生效**
- ✅ **v2.14 对话流对齐 harness 最终版（2026-08-15，用户「再借鉴 harness，样式一模一样」）**：**步骤卡片 → per-tool 平铺**（Timeline 重构：每工具独立一行，harness ToolCallTree 同构，去掉 StepCard 分组）；**工具行**图标映射补齐 36 工具、运行中图标+扫光、失败红点、成功无标记、risk 徽标移除、摘要关键键优先（command/path/query）；**文件路径链接**（read/write/edit/read_files 摘要可点击 → 打开代码界面自动展开定位，store codeViewFile + CodeView 消费；span 防 button 嵌套）；已对齐不动（气泡/操作行/思考行/状态行 shimmer/错误行/代码块/markdown 标题）；保留用户拍板项（字号 15/25、中文「思考」、turn 连续流、状态行文案）。web tsc/vite build + 浏览器实时实测（三工具行平铺/关键键摘要/零 console 错误）
- ✅ **v2.13 逻辑错误与 bug 排查收尾（2026-08-15，双 explore 排查 29 项确认修复）**：**安全类**——命令白名单组合符绕过（`git status && rm -rf` 退回审批 + git branch/config 写操作收窄）、路径 startsWith 前缀漏洞（统一 isPathInside 边界封装替换全站）、git_diff file 参数注入、webfetch SSRF（私网/回环/云元数据拦截）、glob 反斜杠/绝对路径逃逸；**审批一致**——git_commit/edit_file 声明对齐 low、git_add 补 guard；**状态/生命周期**——stopped 终态保护（不被 done 覆盖）、双发 TOCTOU、重复守卫跨轮累计复活、图片降级精确化（视觉特征触发/只动末尾消息/累加器重置）、后台委派上限、后台清理全深度、jobs 双触发、waiters 竞态、附件早退清理；**前端状态路由（并行/队列核心）**——finally/catch 按本连接会话收尾（finishAssistantFor/addErrorFor）、abortController per-session Map、plan/todos/usage 按会话存储+切换回填、重放跳过接管会话+后台只写缓存、队列消费会话 root、队列失败插回、agent-waiting/resumed 线程提示、停止反馈服务端落库。npm test 全绿（新增 bugfix 回归 33 项）+ e2e 实测（组合符走审批/glob 越界拒绝）。**至此 v2 全部阶段完成**，剩余全为触发条件项（见 ROADMAP）
- ✅ **v2.12 工具精简 / usage 四桶 / 会话查询（2026-08-15）**：**工具 schema 精简**（Token 成本杠杆——compactJsonSchema 对全部工具参数 JSON Schema 递归裁剪：删冗余元字段/description 截断 150/enum 截断 12/属性上限 20/深度 >5 折叠；工具 description 截断 800；纯裁剪不影响执行；实测内置工具 schema -27%，MCP 大 schema 收益更显著）；**usage 四桶**（uncached=miss/output=completion/cacheRead=hit，chat 解析 + loop/orchestrator 聚合 + StatsLine「缓存命中 X%（读 N · 未命中 M）· 输出 N tokens」，无缓存数据端点保持估算兜底；ContextMeter 占用率环已存在）；**session 查询工具**（session_search 关键词/最近会话 + session_trace 关键事件轨迹，均 low 进只读白名单；store 可注入防测试污染）。npm test 全绿（新增 v212 套件 16 项）+ 真实模型端到端（session_search 搜历史 → session_trace 复盘 937 事件会话）。**剩余 v2.12+ 全为低优先级/触发条件项**（持久 shell/LSP/read_image 等，见 ROADMAP）
- ✅ **v2.11 子智能体控制 + 后台任务（2026-08-15）**：**子智能体控制**（对齐 Claude Code SendMessage 恢复 + Agent View 仪表盘）——delegate_task 加 `background` 参数（后台模式：立即返回不阻塞，独立 AbortController，父级中止传播）；新工具 **list_agents**（查状态）/ **report**（回收结果）/ **send_message**（恢复等待中的子 Agent）/ **interrupt_agent**（中止一个或全部）；子 Agent 内部 **agent_message**（暂停等父级回复 → agent-waiting 事件 → send_message 恢复 → agent-resumed；仅后台模式防死锁）；per-session 上限 6 后台同生效；父任务结束按委派深度自动中止子 Agent（server/cli finally 挂点）。**后台任务 job**（harness jobs 同款）——run_command 加 `background` 参数（审批/断网门禁与同步一致）；新工具 **job_list / job_output**（环形缓冲 512KB + tail）/ **job_kill**（杀进程树 Windows taskkill /F /T）；每会话上限 8；job-start/job-done 事件落库；审计 soft-bg；安全边界：后台暂走软沙箱语义（L1.5 接口同步无法后台化）。管理工具全 low；list_agents/report/job_list/job_output 进只读白名单。npm test 全绿（新增 subagent-control 24 / jobs 19）+ 真实模型端到端实测（后台 run_command → job 全链 + 后台委派 explore → list_agents → report 回收）。**bug 排查收尾暂缓落档**（用户 2026-08-15 拍板先做 v2.11，触发条件 = 用户提出具体 bug 时再专项排查）
- ✅ **v2.10 工具集/调用机制/Token 优化（2026-08-14）**：**Todo 面板**（todo-write 事件落库 + 输入卡上方折叠条，harness TodoDock 同款）；**AskModal 升级**（multiSelect/推荐徽章/description，对齐 AskUserQuestion）；**压缩优化**（先剪超长工具结果 head4K+tail1K 零成本、摘要调用 = 会话真前缀复用 provider warm KV cache）；**glob 工具**（fast-glob 找路径，只读白名单）；**调用机制**（重复调用 3/5/8 次提醒守卫、run_command 输出 >8K 落盘 .infu-outputs + 回填 head/tail、只读并行池上限 10）。npm test 866 项全绿（tools-opt 12 项新增）；v2.12+ 已规划落档（usage 四桶/schema 精简/session 查询等，用户授权触发条件标注）
- ✅ **v2.9 右侧栏浏览器式改造（2026-08-14）**：**标签页系统**（store rightTabs + tab 条 + 空态初始面板：审查/浏览器/子 Agent/computer-use 待开发）；**子 Agent tab**（subagent-start 自动开 tab + 实时跟随，SubagentViewer 弹窗→tab 内容，委派条目点击打开，状态徽标）；**审查 tab**（DiffPanel 抽 ReviewPane 复用）；**浏览器 tab 占位**（等桌面化做嵌入式真浏览器，ZCode 同款，已记遗留）；**子 Agent 上限 = 每会话 6**（主流 per-session 模型：harness 10/Codex 4/Claude Code 20 均为 per-session；ToolContext.sessionId 传递链 + 活跃计数 + 超限拒绝）。npm test 854 项全绿 + 浏览器实测（含真实委派自动开 tab）
- ✅ **v2.8 批 2 多会话并行 + 排队 + 附件 + @插件（2026-08-14，用户拍板四项）**：**多会话并行**（修「执行中不能切会话」bug——store 重构：running 全局单值 → runningIds[] + sessionCache（每会话消息缓存/流式写对应缓存/切换秒切）+ eventTarget（SSE 按连接会话路由防串扰）；Sidebar 解除运行守卫；服务端 per-session 双发 400 + 启动清残留 running；侧栏运行绿点）；**排队发送**（harness QueueDock 增强：运行中输入入队，dock 条编辑/移除/**立即发送 = Stop&Send**/拖拽排序，done 自动消费队首循环直到空，停止/异常保留队列；消费用会话自己的 root）；**附件**（调研定稿：图片走视觉 + 文件走路径引用；浏览器无绝对路径 → 内容上传暂存 ~/.infu/attachments/<sid>/ 任务后清理；Paperclip 按钮 + 文件/文件夹选择 + 预览条（文件卡片/图片缩略图，单文件 2MB/图片 5MB/20 个上限）；extraReadDirs 只读白名单（read_file 放行/写不放行）；图片 dataURL 视觉仅 Executor；attachments 事件落库可重放）；**@ 插件**（光标 @ 触发面板实时过滤、无匹配自动消失、↑↓/Enter/Esc + 点击即用、插入 @插件id 引用）；**文件选择文案对齐**（浏览文件夹→选择文件夹）；**自由会话 root 保持现状**（不设 defaultRoot = 启动目录可读写，与 Claude Code 一致）；**字体整体缩小一档**（正文 15/25、气泡 15/23、根缩放 sm 15→14px）。npm test 859 项全绿 + 浏览器实测（双会话并行双绿点/排队自动消费 17.5s/附件菜单/@ 面板过滤/字号）
- ✅ **v2.6 收尾：主流 Agent 工具补齐 + Git 优化 + 工具调用优化（2026-08-13，用户拍板「两个都做+加工具+全部工具调用优化」）**：**联网调研** Claude Code/Gemini CLI/opencode/Codex 四家工具集后补齐——**联网工具** webfetch（URL 抓取→文本）/ web_search（DuckDuckGo 免 Key 默认 + INFU_TAVILY_API_KEY 切 Tavily），均走 run_command network=true 同款门禁（high + requireExplicit，-y 不放行，默认断网）；**Git 提交链** git_log（只读）/ git_add / git_commit（本地提交绝不 push）/ git_branch（list/create/switch，分支名白名单）+ git_diff 增强（stat 可关/file 过滤）；**任务协作** read_files（批量读，进只读白名单）/ todo_write（执行阶段清单）/ ask_user（执行中提问：ToolContext.askUser + ask-user 事件 + POST /api/ask/:id + CLI stdin + Web AskModal，子智能体继承）；**工具调用优化四项**（loop.ts）：畸形 JSON 自动修复（围栏/括号/尾逗号/单引号，仍失败回填错误不执行）+ 未知工具列出可用清单 + 写工具串行/只读并行分组（isMutatingTool）+ 工具结果回填 8K 裁剪（事件/落库完整）；结构：公共助手抽到 tools/util.ts，新工具分模块 web/git-tools/task-tools。npm test 全绿（新增 4 套件 51 项）
- ✅ **v2.8 UI 整体打磨（2026-08-14，用户拍板「抄作业 E:\app\deepseek-harness」）**：**定稿基调（用户三项决策）**——主强调 = **中性灰**（主按钮暗色近白 #F9FAFB/浅色近墨 #0F1115；语义色不变：运行/成功绿 #22C55E、链接信息蓝 #679EFE、进行中 #5686FE、警告黄、错误红）；**深+浅双主题**（深色默认，`html[data-theme]` 翻转整套 token，设置→外观切换即时生效，config `appearance.theme` 持久化）；**去顶栏**（三栏通高，Logo/新建/搜索/设置全进侧栏）。**落地**：设计系统重写（`web/src/index.css`：harness 双主题 token + `@theme inline` 旧类名映射 → 全站类名自动双主题；薄滚动条；reduced-motion）；三栏骨架（App.tsx：侧栏 264–420 拖拽 + 56px rail 折叠 + <1024px 自动折叠；右详情 300–520 拖拽 + 可关到 0 + 拖柄点击重开）；侧栏重构（60px Logo 行/新建 38px r12/搜索胶囊/会话树 32px 行状态点/底部设置行/rail 三圆钮）；对话区（空态 Hero 光晕+「探索未至之境」+项目 chip+模板卡；用户右侧 r22 气泡；助手无气泡；思考/工具折叠行 24px + 运行扫光 + IN/OUT r12 卡片；shimmer 运行状态行 + 耗时；回到底部圆钮；`.infu-md` markdown 全套样式 + 代码块粘性语言条）；输入胶囊（r22 悬浮卡 + 34px 圆形发送键↔停止方块 + 模型药丸 + 思考 1–4 + 工作树 chip；工作树条/计划卡改 dock 接管卡）；右详情栏（可拖拽折叠 + r12 卡片 + 复制）；**弹窗统一**（新共享原语 `ui.tsx`：Modal r24/CapsuleButton/Toggle/StateDot/DisclosureRow/CodeBlock；审批/提问/归档/创建项目/设置六弹窗收敛；SettingsModal 改自适应 r24 双栏 188px 导航轨）；**修 bug**：设置弹窗固定尺寸小屏不可用、`E:\InFu(test)` 硬编码、`#38bdf8` 硬编码→token、ModelPane 原生 confirm()→统一 Modal、死代码清理（ToolCard/ComingSoonPane/未用导出）；终端 xterm 配色随主题热切换。**注意**：shared 包 appearance schema 已加 `theme`（dist 已重建），server 的 PUT /api/config 用 strip() 模式——**运行中的旧 server 进程需重启**才接受 theme 落盘。验证：`npm test` 全绿 + web tsc/vite build + 浏览器实测（折叠/拖拽/主题即切即生效/设置保存/theme 持久化回读，详见 ROADMAP v2.8 节）

## 未完成 / 下一步

**以 `docs/ROADMAP.md` 为准**（本处仅摘要）：
- ✅ **桌面端已全部完成**（v3.0 全批次：Electron 壳/嵌入式真浏览器/CDP 桥/computer-use/打包；2026-08-16 收官）。**单机增强已全部落地**（定时任务/语义检索/持久 shell/LSP/记忆剪枝/computer-use/read_image/审批全权放行/read-before-edit/异步任务编排/审批 full 档/数据生命周期/autoCommit/autoRefine，2026-08-17）。**测试/文档已齐（v3.8）**：43 套件 1286 断言全绿（scripts/test-runner.ts 统一跑）。**剩余触发项**：⏳ 正式图标（用户提供后替换 build/icon.png 重新打包）；⏳ UI 树读取 accessibility tree（高成本拆分后续）；⏳ 沙箱 microVM（多租户触发）；⏳ v3 团队版（第二个真实用户触发）、WSL2 原生沙箱

## 项目约定（用户明确要求，2026-08-12）

- **UI 决策必须先讨论后动手**：v2.1 设计系统升级、v2.3 整体打磨，以及任何新页面/新交互的视觉方案，动手前必须先与用户讨论定稿（用户强调"到时候必须讨论"）
- **测试模型固定用 agnes（agnes-2.5-flash）**：所有 InFu 端到端测试/实测默认用 agnes 模型，**不许用 deepseek**（用户 2026-08-1- ✅ **v2.6.1 会话中枢重构（2026-08-13，用户纠正「任务=会话」误称后定稿）**：**概念修正**——会话是核心对象、项目是容器、任务看板删除；**记忆系统四层定稿**（「发生的事」会话历史 /「总结」项目历史自动沉淀 /「下次该怎么干」项目·全局记忆 /「必须遵守」INFU.md；L3 任务记忆删除；自由会话按 root 解析记忆 = 全局 + root 下 .infu/memory；生成时机 = 会话中 Agent 主动写（Claude Auto Memory 模式）+ 会话结束自动归档（Codex 即时简化版）；**memory_write 敏感凭据检测**（Codex secret-redactor 轻量版：sk-/AKIA/私钥/Bearer/连接串/JWT 拒绝写入））；**会话管理**（sessions 表幂等迁移 pinned/archived + `PATCH /api/sessions/:id` 重命名/顶置/归档 + 列表 archived 过滤）；**项目注册表**（`~/.infu/projects.json`：创建校验/查重/损坏备份；会话按 root 命中判断隶属；**移除只删注册**，会话保留为自由、文件夹不删）；**侧栏会话中枢**（用户定稿：顶部 新建会话 CTA + 定时任务[规划中]/技能（位置不变）+ 搜索；Archive + 全部收起；已顶置区；项目区（折叠全部/创建项目/项目行[移除][新建会话]/组内会话[重命名/顶置/归档]/显示更多）；自由会话区；Archive 弹窗恢复/删除；创建项目弹窗 = 路径输入 + 历史 root 选择（Web 无法读文件夹绝对路径））。`npm test` 全绿（memory 85 + projects 21 + session-store 30；tasks 59 已删）+ API 端到端（创建项目→归属→顶置/重命名/归档→移除保留会话）+ 浏览器（侧栏渲染/弹窗/cua 点击；IAB broker 故障降级 cua+API 验证）。**遗留（v2.7）**：记忆索引/剪枝、后台提炼管道（Codex 6h 模式）
- ✅ **三档模式移除（2026-08-13）**：「编排/直接/方案」三档整体删除（权限维度 = 设置页全局审批档位已覆盖；流程维度交 AI 自适应，v2.6.5 优化）——删 Web 模式选择器/CLI `--suggest` `--no-orchestrate`/suggestOnly 方案模式/直接模式分支；唯一流程 = Planner→计划确认→Executor→Reviewer；顶栏精简（模型选择/root 输入框移除，仅留设置+运行状态）
- ✅ **v2.6.2 交互修复（2026-08-13）**：① **寒暄短路**——Planner 提示词第 0 条判断消息类型 + orchestrator 短路（未调工具且无【建议步数】→ 跳过确认/执行/审查直接回复；实测「嗨」9.4s 纯文本 0 工具）② **项目行点击 = 选中切 root**（折叠独立为 ⏷ 按钮；选中项目后顶部「新建会话」即在项目下新建）③ **/api/chat root 校验**（空/不存在目录立即报错，不再静默空转）+ 前端初始 root 置空（移除 E:\InFu(test) 占位，挂载用 defaultRoot 填充）
3 明确要求）
- **v2 聚焦单机个人化**：团队/公司版（v3）触发条件 = 出现第二个真实用户或明确团队需求，届时再讨论
- **记忆系统形态**（项目记忆/任务总和记忆/全局记忆三层）与**模型选择流程**（codex 式）细节，实施前单独讨论定稿

## 安全红线（不可妥协）

- API Key 只存 `~/.infu/config.json`（0600），严禁入库、严禁进沙箱环境
- 沙箱写保护：`~/.ssh`、`~/.infu`、`~/.aws`、`~/.gnupg`、`~/.docker` 永远只读
- 审批提示必须展示解析后的真实绝对路径（防 CWE-451）
- 高风险命令（rm -rf/format/mkfs/dd）必须 high 级审批
